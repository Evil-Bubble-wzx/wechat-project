# 听阅小程序：技术架构与需求总览

> 版本：v1.2 ｜ 修订：2026-09-16 ｜ 取代 v1.1
> 目标：复刻 MegaWords 的**数字闭环**，纯数字化实现，零版权成本
> 定位：英语阅读学习工具，**音频为配套材料**（不是听书平台——这关系到类目与资质）
> 工程约束：交付一个边界清晰、可嵌入其他微信小程序的独立业务模块；新增书籍和音频不得依赖改代码、重新发版

---

## v1.2 修订：范围从「只做听阅」扩到「数字闭环全拉通」

依据 MegaWords 7 张真实截图（2026-09-16）复盘，原 v1.1 只覆盖 R1–R9（听阅核心），缺了 Tab 容器、详情页、最近播放、Quiz、报告、排行、个人中心。经确认，本期范围调整为：

| 决策项 | 结论 |
|---|---|
| **复刻范围** | **数字闭环全拉通**：首页找书 → 详情 → 听 → 测 → 报告 → 排行 → 我的。**借阅 / 线下馆 / 借阅卡不做** |
| **排行榜** | **做**。指标 = **完成篇数（主）+ 累计时长（副）** |
| **Quiz 与家长报告** | **拉回本期，两个都做**（v1.1 原定「暂缓但留数据」作废） |
| **目标年龄段** | **两端都要，先覆盖低龄**：交互按 4–8 岁做，内容分级铺到 AR 4–5，后期按数据分化 |
| AR 值获取 | 公开工具手查（AR BookFinder / Lexile Find a Book），只建索引不搬题库 |

### 一句话差异（v1.2 核心判断）

**MegaWords = 借阅履约（重）+ 听阅（轻）；我们 = 借阅履约（无）+ 听阅（重）+ 测 + 报告 + 排行。**

"复刻"成立的是**数字闭环**那一半。物理借阅（Call Slips 四态、Library 1003 / 有效期、线下馆营业时间）是它的成本中心也是护城河，不是我们的复制对象。

### 三条从截图读出、v1.1 没有的产品判断

1. **详情页把「Read for Me」与「Take Quiz」并排成两个大 CTA** —— "先听后测"被显式写在 UI 层，不是埋在流程里。听和测是一对，缺一个产品立不住。这是 Quiz 必须拉回本期的直接理由。
2. **排行榜计量单位是"词数"不是时长** —— 时长奖励"挂着不动"，词数奖励"真的读完"。我们改用**完成篇数 + 累计时长**双指标，因为它不依赖 Quiz 通过即可统计，实现更轻。
3. **底部 Tab 只有三个：Home / Recent / Me** —— 信息架构围绕"我的进度"而非"内容库"组织。这个克制值得照抄。

### 明确不做（本期）

| 不做 | 原因 |
|---|---|
| 物理借阅 / Call Slips 四态 | 重履约，与纯数字化定位冲突 |
| 借阅卡（Library N / 有效期） | 同上，依赖实体馆藏 |
| 线下馆营业时间、本馆维度 | 无线下 |
| 外教 / 人工点评 | 用 AI 替代，这是我们的差异点 |
| 纸质书物流 | 同上 |

### 待定（未拍板，v1.2 先留接口不实现）

- **社群感**：MegaWords 有「1805 人已参与」横幅 + 「Congratulations!」学员头像轮播 + 「本馆」地域维度。纯数字化版要不要做、做到什么粒度，未定。
- **邀请好友 / 支付记录 / 券包**：属商业化模块，取决于定价模型（v1.1 §11 待决策第 3 条）是否先定。
- **笔记**：截图里笔记按钮是橙色高亮的，疑似被强调。是否做「读完写一句话」未定。

---

## 0. 产品定位

| 项 | 决定 |
|---|---|
| 一句话 | 让孩子真正读完一篇英文经典，而不是刷完 100 本分级小书 |
| 与 MegaWords 的差异 | 不做纸质借阅、不做线下馆、不做重人工；用 AI 替代人工解读 |
| 与 ABC Reading 的差异 | 不做泛读刷量，做**深度精读单篇** |
| 内容单位 | **「篇」而非「本」**——合集里的单篇、长篇的单章，都是独立单元 |
| 核心体验 | 找书 → 听整篇（沉浸）→ 看字幕（可选，默认关）→ 点词查义（自动暂停）→ 做 Quiz → 报告看见进步 → 排行被激励 |
| 目标年龄 | 4–12 岁，交互先按 4–8 岁做 |

### 关于"内容单位"：它不是地基，是粒度决策

**播放层确实只认一个 URL**，播放器不关心它叫"篇"还是"本"。之所以按「篇」而非「本」组织，是因为另外五处受影响：

| 受影响处 | 单位是「本」 | 单位是「篇」 |
|---|---|---|
| 缓存粒度（硬约束） | 200MB 一本，缓存不了 | 5MB 一篇，可缓存 |
| 数据下发（硬约束） | 整本字幕+词表几十 MB | 一篇几百 KB，秒开 |
| 进度记录 | "读了 0.2 本"，无完成感 | "读完第 7 篇" |
| Quiz | 9 小时的书做 8 道题不合理 | 一篇 8 道题，测得住 |
| **排行榜** | "本月读了 0.3 本"无法排名 | "读完 6 篇"可直接排序 |

**实现方式**：`book` 只是分组容器，URL 挂在 `piece` 上，做成两级。成本几乎为零。

**什么时候不重要**：纯在线 + 不记进度 + 不做 Quiz + 不做离线，平铺列表即可。
**什么时候必须定死**：只要做**离线缓存**或**按篇 Quiz**或**排行榜**，单位必须先定——下载管理、题目生成、榜单统计都按单位走，事后改要重写。

---

## 1. 需求清单

### 1.1 功能需求（v1.2 全量）

> 优先级：P0 = 本期必做；P1 = 本期视排期；P2 = 明确不做或待定

#### A. 骨架与导航

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| F22 | **底部 Tab 容器（Home / Recent / Me）** | P0 | 照抄 MegaWords 的三 Tab 克制结构 |
| F1 | 首页 / 篇目列表，按难度排序 | P0 | 显示难度等级、时长、状态 |
| F16 | **搜索（Title / Series / Author / No）** | P0 | 全字段检索 |
| F24 | **F/NF 分流（虚构 / 非虚构）** | P0 | MegaWords 显式做的两条线，成本极低 |

#### B. 内容详情

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| F17 | **篇目详情页** | P0 | 封面 + 标题 + 作者 + Lv + **状态** |
| F17.1 | 元数据四件套 | P0 | No. / Words / Lv / F-NF / Pages |
| F17.2 | 篇目三态 | P0 | **未开始 / 进行中 / 已完成**（替代 MegaWords 的"可借/借过"） |
| F17.3 | **双 CTA** | P0 | **Read for Me**（听）/ **Take Quiz**（测）并排 |
| F17.4 | SERIES 同系列列表 | P0 | 照抄 MegaWords 的 "SERIES (25)" 横向列表 |
| F23 | 收藏 / 点赞 / 笔记 | P1 | 笔记是否做未定，见 §待定 |

#### C. 播放

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| F2 | 音频在线播放（边下边播） | P0 | `BackgroundAudioManager` 直接播 URL |
| F3 | **后台播放 + 锁屏控制** | P0 | 车上/睡前场景刚需；必须设 title/singer/coverImgUrl |
| F4 | 连续播放 | P0 | `onEnded` 自动切下一篇 |
| F18 | **播放器完整控件** | P0 | 大封面 + 循环三态（单曲/顺序/列表）+ 倍速 + 上/下一首 + 播放列表 + 秒级进度 |
| F19 | **最近播放列表** | P0 | 时间倒序，显示 (No., Lv, Words) + 日期 |
| F5 | 句级字幕同步高亮 | P0 | **默认关闭**，可一键开启；**5–7 行渲染窗口** |
| F6 | 点词查义（音标+词性+中英释义+原句） | P0 | **自动暂停**；长按触发，单击发音/高亮 |
| F7 | 生词本 | P0 | 点过的词自动收集 |
| F12 | 词级卡拉 OK 高亮 | P1 | 需 50ms 轮询 `currentTime`，配 WXS |

#### D. 测与报告

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| F8 | **每篇 5–8 题 Quiz** | P0 | 四层题型；≥85% 建议升级，<60% 建议降级 |
| F9 | **家长报告 / 我的 Quiz** | P0 | 照抄 MegaWords 的 STATS 块结构（见 §3.4） |
| F25 | **定级测试（TEST AGAIN）** | P1 | 重新定级入口，是推动升级与续费的钩子 |

#### E. 激励与账户

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| F20 | **排行榜** | P0 | **完成篇数（主）+ 累计时长（副）**；7 日榜 / 总榜两档 |
| F21 | **个人中心** | P0 | 累计三件套（BOOKS / WORDS / LISTENING）+ 阅读级别 + 菜单骨架 |
| F10 | 阅读进度同步 | P0 | 跨设备、中断续听 |
| F11 | **离线播放** | P1 | 下载到小程序本地文件，上限 200MB |
| F13 | 跟读打分 | P1 | |
| F14 | 按水平推荐篇目 | P1 | |
| F15 | 打卡与连续天数 | P1 | |

### 1.2 非功能需求

| 项 | 指标 |
|---|---|
| 音频首播启动 | ≤ 1.5s |
| 字幕同步误差 | ≤ 300ms（句子级） |
| 点词响应 | **0 延迟**（本地预取命中） |
| 离线可用性 | 已缓存篇目 100% 可离线听 |
| 本地占用 | 常驻 ≤ 100MB（LRU 自动清理） |
| 弱网表现 | 音频可边下边播，字幕/词表异步拉取不阻塞播放 |
| 排行榜刷新 | 榜单数据可容忍 5–10 分钟延迟（走缓存，不必实时） |

---

## 2. 技术架构

```
┌─ 宿主小程序 ──────────────────────────────────────┐
│ 登录/用户标识 · 路由入口 · 主题样式 · 云环境配置    │
│                 ↓ 通过 Adapter 注入                │
├─ 听阅功能模块 modules/listen-read ────────────────┤
│ Tab 容器（Home / Recent / Me）                     │
│ pages 页面 · components 组件 · player 播放内核      │
│ content 内容访问 · progress 进度 · cache 离线缓存   │
│ quiz 测验 · report 报告 · ranking 排行             │
│ 模块内只依赖统一接口，不直接耦合宿主业务             │
├─ 内容与业务服务 ──────────────────────────────────┤
│ 内容 API · 词库服务 · 进度同步 · Quiz · 报告 · 排行 │
├─ 内容导入管道 tools/content-import ───────────────┤
│ 校验 manifest → 转码/上传 → 生成时间戳/词表/题目    │
│ → 写库/发布；新增内容不修改小程序代码               │
├─ 存储与分发 ──────────────────────────────────────┤
│ 对象存储 + CDN（音频/封面）· 数据库（元数据/进度）  │
└───────────────────────────────────────────────────┘
```

### 2.1 模块边界与移植方式

建议把功能放在固定目录 `modules/listen-read/`，不要散落到宿主项目的 `pages`、`utils` 和全局状态中。模块对宿主只暴露少量接口：

```ts
interface ListenReadHostAdapter {
  getUserId(): Promise<string>
  request<T>(options: RequestOptions): Promise<T>
  getTheme(): { primaryColor: string; textColor: string }
  navigateBack(): void
  reportEvent(name: string, data?: Record<string, unknown>): void
}
```

移植到另一个微信小程序时，只需：

1. 复制 `modules/listen-read/`（后续稳定后可改为 Git subtree 或 npm 私有包，但首版不必过度工程化）；
2. 在宿主 `app.json` 注册听阅页面或分包；
3. 实现一个宿主 Adapter，接上现有登录、请求封装、主题和埋点；
4. 配置 API/CDN 地址与功能开关；
5. 完成播放、登录态、返回路径和真机后台播放的回归测试。

**禁止耦合项**：模块不得直接读取宿主全局 store 的内部字段，不得写死另一个项目的用户表，不得依赖宿主页面路径，不得在页面里写死书目和音频 URL。

**v1.2 新增约束**：排行榜、报告、Quiz 三个新子模块同样放在 `modules/listen-read/` 内，共用同一套 Adapter 与内容 API，不得各建一套用户体系或请求封装。

### 2.2 内容扩展方式

每本书使用同一种内容包结构：

```text
content/<book-id>/
├─ manifest.json       # 书籍元数据、章节顺序、版本号
├─ cover.jpg
└─ audio/
   ├─ 001.mp3
   ├─ 002.mp3
   └─ ...
```

`manifest.json` 示例：

```json
{
  "schemaVersion": 1,
  "book": {
    "id": "the-secret-garden",
    "title": "The Secret Garden",
    "author": "Frances Hodgson Burnett",
    "cover": "cover.jpg",
    "level": "L3"
  },
  "pieces": [
    {
      "id": "chapter-01",
      "index": 1,
      "title": "There Is No One Left",
      "audio": "audio/001.mp3"
    }
  ]
}
```

导入命令只接受内容目录，不要求编辑源码，例如：`content-import validate`、`content-import publish`。发布流程应自动完成：格式校验、音频探测与统一转码、计算时长和体积、上传对象存储、生成 CDN URL、生成或导入字幕/词表/Quiz、写入数据库、输出导入报告。

内容需有 `draft / published / archived` 状态和 `content_version`。客户端只读取已发布内容；修改音频或字幕时提升版本号，缓存按版本失效。这样新增和修订书籍都不需要重新发小程序版本。

**技术选型**

| 层 | 选择 | 理由 |
|---|---|---|
| 前端 | 原生微信小程序 + 独立业务模块 | 当前目标是移植到其他微信小程序；不为跨端迁移提前付出成本 |
| 模块集成 | 固定目录 + Host Adapter + 独立配置 | 隔离登录、路由、主题和请求差异，避免绑定某个宿主项目 |
| 后端 | 统一内容 API；部署可先用 CloudBase | 客户端依赖接口契约而不是 CloudBase SDK 细节，便于复用 |
| 音频 | 云存储 + CDN | 主包仅 2MB，音频必走网络 |
| 本地缓存 | `FileSystemManager`（非 `setStorage`） | 后者仅 10MB，装不下音频 |
| 内容维护 | manifest + 导入脚本 + 管理状态 | 新增书和音频不改代码、不发版，可批量校验与发布 |
| 排行榜 | 服务端定时聚合 + 缓存 | 容忍 5–10 分钟延迟，避免每次进榜全表扫描 |

---

## 3. 关键技术方案

### 3.1 音频播放：三条链路

| 场景 | 实现 |
|---|---|
| **在线** | `BackgroundAudioManager.src = CDN URL`，边下边播 |
| **离线** | `wx.downloadFile` → `saveFile` → src 用本地 `wxfile://` 路径 |
| **后台** | 必须 `BackgroundAudioManager`（`innerAudioContext` 一切后台即停）；`app.json` 声明 `requiredBackgroundModes: ["audio"]`，并向微信申请接口权限 |

⚠️ 已知风险：**iOS 上 `BackgroundAudioManager` 播本地文件的兼容性有社区反馈**，必须真机实测。

**播放器控件（F18）**：循环三态与倍速是 `BackgroundAudioManager` 的能力边界——循环三态需自己维护播放队列（`onEnded` 决定下一首），倍速用 `playbackRate`（取值 0.5/0.8/1.0/1.25/1.5/2.0）。

### 3.2 字幕同步

```js
cues = [{ start: 52, end: 84, text: 'You have been my friend.', zh: '你一直是我的朋友。' }]

ctx.onTimeUpdate(() => {                    // 约 250ms
  const i = cues.findIndex(c => ctx.currentTime >= c.start && ctx.currentTime < c.end)
  if (i !== last) { last = i; this.setData({ currentIndex: i, scrollTo: `line-${i}` }) }
})
```

- **5–7 行是渲染窗口而非样式**：`cues.slice(i-2, i+5)`，共 7 个节点；当前句高亮放大、前 2 句次级灰、后 4 句正常
- **切句直接换内容，不做滚动动画**（句子长短不一，动画必闪）
- 只 `setData` 一个 `currentIndex` + 窗口数组，绝不整列更新
- 词级高亮需更高精度：`ctx.currentTime` 可随时主动读取 → `setInterval(50)` 轮询 + WXS 更新

### 3.3 点词查义：预翻译 + 批量预取 + 本地缓存

**分词**（小程序特有坑：`rich-text` 的 tap 不冒泡，禁用）

```js
const tokens = sentence.split(/([A-Za-z']+)/)
  .map(s => s.match(/[A-Za-z']+/) ? { type: 'word', v: s } : { type: 'punct', v: s })
```

**策略**：服务端共享词库 + 进篇时批量预取（一个请求拉本篇全部释义，约 15KB）+ 本地命中零延迟 + 未命中走单条请求并回写。

**服务端绝不能实时调 LLM**：首次生成后入库，之后纯 DB 查询。否则 1000 DAU × 30 词/天 = 每天 3 万次 LLM 调用。

**五个必处理细节**：
1. 词形还原（children→child、ran→run）
2. **释义本身的难度控制**——英文释义用词须落在 2000 基础词内，否则释义比原词还难
3. 多义词只给一个义项——**必须把原句作为上下文喂给 LLM**
4. 专有名词标记跳过（Charlotte / Wilbur）
5. 停用词不响应

**AR < 2 出图片**：`level < 2 且 vocab[lemma].img 存在` → 图片；否则回退文字释义。只给 150–300 个高频具象名词配图，动词与抽象词必须有文字回退。

### 3.4 Quiz 与家长报告

**Quiz（F8）**
- 每篇 5–8 题，四层题型：字面 → 推断 → 主旨 → 词汇语境
- 构建期 AI 生成 + 人工抽检（抽检比例 ≥10%，错误率控制在 5% 内）
- 判定规则：≥85% 建议升级、<60% 建议降级

**家长报告（F9）** —— 照抄 MegaWords 截图中的 STATS 结构

| 指标 | 说明 | 数据来源 |
|---|---|---|
| Total Books / Pieces | 累计完成篇数 | `user_progress` where completed |
| Total Words | 累计词数 | Σ `pieces.word_count` |
| **Early** Correct% / Level | 早期（如前 1/3 时段）表现 | 时间切片聚合 |
| **Rct**（Recent）Correct% / Level | 近期表现 | 时间切片聚合 |
| Avg Correct% / Avg Level | 全程平均 | |
| Fiction% | 虚构类占比 | `pieces.fn` |
| Total Score | 综合分 | 加权 |
| 派生 | 当前年级 / 阅读级别 | 由 Avg Level 映射 |

⚠️ **Early / Recent 的对比是家长报告的价值核心** —— 它让家长看见"进步"而不只是"数量"。MegaWords 把 PASSED 与 ALL 做成两列对比，我们至少要保留 Early vs Recent 这一组。

### 3.5 排行榜（F20）

| 项 | 决定 |
|---|---|
| 主指标 | **完成篇数**（`completed` 的 piece 计数） |
| 副指标 | **累计时长**（Σ 有效播放秒数） |
| 周期 | 7 日榜 / 总榜（**无"本馆"维度**，我们没有线下） |
| 自己的位置 | 底部固定显示自己排名 + 数字（照抄 MegaWords） |
| 实现 | 服务端定时聚合到缓存表，接口只读缓存；容忍 5–10 分钟延迟 |
| 排序 | 先按完成篇数降序，同分按累计时长降序 |

⚠️ **时长指标必须防挂机**：只统计 `onTimeUpdate` 真实推进的秒数，且单日单篇计入上限不超过该篇 `duration_sec` 的 1.2 倍。否则排行榜会被挂机刷烂，且会诱导错误行为。

### 3.6 离线缓存策略

- 只保留**当前篇 + 下一篇**，听完即删，LRU 兜底
- 预下载默认仅 WiFi
- 文件被系统清理 → 检测不存在则重下（需 loading 提示）

### 3.7 部署

开发版 → 体验版 → 提交审核（1–2 天）→ 分阶段发布 → 全量（可回退）。
`miniprogram-ci` 接 CI/CD。主包 ≤2MB、总包 ≤20MB。

---

## 4. 数据模型

| 表 | 字段 |
|---|---|
| `books` | id, title, author, year, pd_status, cover_url, level_tier, series_id, **fn**（fiction/non-fiction）, status, content_version, published_at |
| `pieces` | id, book_id, index, title, audio_url, audio_hash, duration_sec, size_bytes, word_count, pages, level, **fn**, status, content_version |
| `cues` | piece_id, [{start, end, text, zh}] |
| `vocab` | piece_id, {word: {lemma, ph, pos, zh, en, img, type}} |
| `quiz` | piece_id, [{q, options, answer, type}] |
| `user_progress` | user_id, piece_id, progress_sec, **listen_sec**（防挂机）, completed, quiz_score, quiz_passed, last_at |
| `user_vocab` | user_id, word, looked_at, count |
| `user_favorite` | user_id, piece_id, created_at（F23 收藏） |
| `ranking_cache` | period（7d/all）, user_id, nickname, avatar, pieces_done, listen_sec, rank, updated_at |

`books` 和 `pieces` 的 ID 必须由内容包显式提供且长期稳定，不能使用数组下标或数据库自动 ID 作为跨环境标识。用户进度依赖这些稳定 ID，才能在不同宿主小程序和不同环境中正确迁移。

**v1.2 新增**：`pieces.fn`、`user_progress.listen_sec`、`user_progress.quiz_passed`、`ranking_cache`。

---

## 5. 接口设计

| 接口 | 说明 |
|---|---|
| `GET /books` | 书目列表（支持 `?fn=fiction&sort=level&q=关键词`） |
| `GET /books/:id/pieces` | 篇目列表（含时长、体积、难度） |
| `GET /pieces/:id/manifest` | **一次下发本篇 cues + vocab + quiz** |
| `GET /pieces/:id/quiz` | 题目（可并入 manifest） |
| `POST /quiz/submit` | 提交答案，返回得分与升降级建议 |
| `POST /vocab/batch` | 批量取词（未命中时 LLM 生成并入库） |
| `POST /progress` | 上报进度 / 播放秒数 / Quiz 结果 |
| `GET /report/me` | 家长报告 STATS 块 |
| `GET /ranking?period=7d` | 排行榜（读缓存） |
| `GET /recent` | 最近播放 |

接口返回应带 `schema_version` 和 `content_version`。模块只能通过内容 API 读书目，不能把书单编译进前端代码。若多个宿主小程序需要共享学习记录，服务端增加 `external_user_id + app_id` 身份映射；若暂不共享，则各宿主只需实现各自的 `getUserId()`，听阅模块无需改动。

---

## 6. 内容源与版权

**唯一无枷锁的起点：公版书 + LibriVox。**

- 美国 1930 年及以前出版的作品已入公有领域；按中国"终身+50年"计算同样过期
- LibriVox 录音为 CC0，可自由使用与商用
- 分级：AR 值与 Lexile 值可从公开工具查（AR BookFinder、Lexile Find a Book），**只建索引，不搬题库**
- Quiz：AI 生成，公版原文可自由使用

⚠️ 三条红线：
1. 不用现代出版社的插图版/注释版（有独立权利）
2. 迪士尼化视觉形象有商标保护
3. **翻译权是独立的著作财产权**——保护期内的作品，翻译并展示比展示原文更敏感

---

## 7. 容量与性能：真实数据

以下时长来自 LibriVox API 实测（2026-09-16）。体积按 32kbps 单声道 = 4 KB/s 计算。

| 作品 | 总时长 | 篇/章 | 单篇均时长 | 单篇(32k) | 整部(32k) |
|---|---|---|---|---|---|
| Just So Stories | 3.6 h | 12 | 17.7 min | 4.2 MB | 50 MB |
| Peter Pan | 5.1 h | 17 | 17.9 min | 4.2 MB | 71 MB |
| Treasure Island | 7.5 h | 26 | 17.3 min | 4.1 MB | 106 MB |
| Anne of Green Gables | 10.5 h | 38 | 16.6 min | 3.9 MB | 148 MB |
| The Jungle Book | 5.0 h | 11 | 27.5 min | 6.4 MB | 71 MB |
| The Railway Children | 5.0 h | 14 | 21.5 min | 5.0 MB | 71 MB |
| The Blue Fairy Book | 13.9 h | 37 | 22.5 min | 5.3 MB | 195 MB |
| A Little Princess | 7.6 h | 19 | 24.0 min | 5.6 MB | 107 MB |
| The Secret Garden | 9.1 h | 27 | 20.3 min | 4.8 MB | 129 MB |
| White Fang | 8.5 h | 25 | 20.3 min | 4.8 MB | 119 MB |
| The Wind in the Willows | 6.5 h | 12 | 32.5 min | 7.6 MB | 91 MB |
| The Prince and the Pauper | 6.9 h | 20 | 20.8 min | 4.9 MB | 97 MB |
| The Call of the Wild | 3.4 h | 6 | 33.9 min | 8.0 MB | 48 MB |
| Robinson Crusoe | 13.9 h | 20 | 41.8 min | 9.8 MB | 196 MB |
| Little Women | 19.0 h | 47 | 24.2 min | 5.7 MB | **267 MB** |

**关键结论：**

- 小程序本地文件上限 **200MB** → 除 Little Women、Robinson Crusoe、Blue Fairy Book 外，多数作品整部可离线
- **单篇普遍 4–8MB** → 按「篇」缓存是正确粒度，20 篇约 100–160MB
- 转码建议：**ffmpeg 批量转 32kbps 单声道**，比源文件小一半以上，转一次永久复用
- 流量：32kbps 听 1 小时约 14MB

**LibriVox 已确认可用**：柳林风声、秘密花园、金银岛、丛林之书、绿山墙的安妮、蓝精灵童话集、彼得潘、小妇人、野性的呼唤、白牙、鲁滨逊漂流记、王子与贫儿、原来如此的故事、铁路边的孩子们、小公主、小火柴女孩(安徒生合集)、绒布小兔。
**需人工确认**：Peter Rabbit、Black Beauty、Aesop、Tom Sawyer、绿野仙踪、Grimm、Gulliver（API 标题检索未命中，不代表没有）。

⚠️ **目标年龄"先覆盖低龄"与内容选择冲突**：上表多为章节书（单篇 17–42 分钟），对 4–8 岁偏长。**低龄段需要单独找短篇素材（5–10 分钟）**，不能只用上表的章节书。这是 v1.2 尚未解决的内容缺口。

---

## 8. 合规与风险

| 风险 | 说明 | 应对 |
|---|---|---|
| **类目认定** | 以音频为核心可能被归为"音频/听书"而非"教育"，资质要求不同 | 明确"英语阅读学习工具，音频为配套"；不做"电台/听书"栏目 |
| 版权 | 翻译权、音频权、改编权各自独立 | 只用公版；任何现代版本先查权利 |
| K12 学科培训红线 | 双减下学科培训受限 | 定位"阅读素养/工具"，不做提分课、外教课 |
| 效果承诺 | 承诺提分会被打脸且有合规风险 | 只承诺"读了多少、坚持多久" |
| 备案与认证 | 小程序强制 ICP 备案；企业认证 300 元/年 | 提前办 |
| UGC 内容安全 | 如有书评/打卡发言 | 接 `msgSecCheck` |
| **未成年人个人信息** | 排行榜要展示昵称与头像 | 默认脱敏（昵称截断 + 可选头像），提供关闭入口；遵守个保法对儿童信息的要求 |

---

## 9. 部署与运营

- **发布流水线**：开发版 → 体验版 → 审核 → 分阶段发布 → 全量（可回退）
- **触达能力弱**：订阅消息一次授权只发一条 → 通知能力必须建在企微/公众号，不在小程序
- **迭代被审核卡**：文案、价格、开关全放服务端配置，尽量不发版
- **增长入口**：搜一搜 SEO、公众号关联、分享卡片、带参小程序码
- **冷启动与排行榜矛盾**：用户量小时榜单只有几个人，观感差。建议榜单在活跃用户 <50 时只显示"自己 vs 自己"，达标后再开放全站榜

---

## 10. 里程碑（v1.2，6–8 周）

| 阶段 | 目标 |
|---|---|
| 第 1 周 | 模块骨架 + Tab 容器 + Host Adapter + 内容 manifest 与 API 契约；**用 1 篇跑通全链路（能播 + 有字幕 + 能点词）** |
| 第 2 周 | 首页 / 列表 / 搜索 / F-NF 分流 + 篇目详情页（双 CTA + SERIES） |
| 第 3 周 | 播放器完整控件（循环三态 / 倍速 / 列表）+ 最近播放 + 进度同步与续听 |
| 第 4 周 | **Quiz**（出题 + 作答 + 判定） |
| 第 5 周 | **家长报告** STATS 块 + **排行榜**（完成篇数 + 时长，含防挂机） |
| 第 6 周 | 个人中心 + 累计三件套 + 真机双端验收与调优 |
| 第 7–8 周 | 内容铺量到 20 篇 + 可选 P1（离线 / 跟读 / 推荐 / 打卡）；做一次真实移植演练 |

**关键闸门**

- **M1（第 1 周末）**：1 篇打通全链路 —— 能播、有字幕、能点词。过不了就停下来修数据链路，不要继续往下堆。
- **M3（第 4 周末）**：Quiz 可作答并出分。这是"数字闭环"是否成立的第一个真检验。
- **M5（第 6 周末）**：真机双端（iOS + Android）后台播放验收通过。

**工程验收线**：

- 在不修改听阅模块业务源码的前提下，能通过 Adapter 接入第二个小程序；
- 新增一本符合规范的书，从放入内容目录到测试环境可见，只需运行导入/发布流程；
- 新增书籍不需要修改前端书单、页面路由或数据库表结构，也不需要重新提交小程序审核；
- 替换某章音频后，`content_version` 更新，旧缓存可被准确识别并刷新；
- 内容导入失败时给出具体到书、章节和字段的错误报告，不产生半发布状态。

**判定线**（提前定死，不靠加功能自欺）：
- 内测家庭 7 天内完成 ≥1 篇 + 1 次 Quiz 比例 ≥50%
- 次日留存 ≥30%，7 日留存 ≥15%
- ≥3 位家长主动问"怎么付费/有没有更多篇"
- ≥1 位家长愿付 99 元/年

---

## 11. 待你决策（v1.2 剩余）

1. **开发资源**：自研还是外包（决定 8 周还是 3 个月）
2. **试点篇目**：第 1 篇用什么内容（卡住 T1.1，最优先）。低龄段需要 5–10 分钟短篇，现有 LibriVox 清单多为章节书，需单独找素材
3. **AR<2 配图来源**：开源图标库 / 公版原版插图 / AI 批量生成 / 本期不出图
4. **定价**：免费+增值 / 订阅 99–199 元/年 / 按篇买断
5. **社群感做不做**：「N 人已参与」横幅 + 「Congratulations!」学员轮播
6. **笔记做不做**：截图里笔记按钮被橙色高亮强调，疑似核心动作
7. **低龄短篇素材来源**：现有公版章节书对 4–8 岁偏长，需要另找（如分级读物改写、短篇童话、或接受 15 分钟单章）

---

## 附：本文档依据

- `MegaWords小程序竞品调研.md` —— 高价重模式拆解 + PC 端实测
- `ABC Reading与英文Quiz体系调研.md` —— 低价数字化路线 + Quiz 品类空档
- `英文原版阅读小程序起步方案.md` —— 内容源、成本模型、冷启动
- `MegaWords-功能盘点与对比分析.md` —— 7 张真实截图的功能逐项拆解与维度对比
- `tools/librivox_check.py` —— LibriVox 音频可用性实测脚本
