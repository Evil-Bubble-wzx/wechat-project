# X-02 iPhone 真机验收记录

> 日期：2026-09-24  
> 状态：`BLOCKED_B_AUDIO_DELIVERY`  
> 目的：关闭 `X-02 断点与可靠完成判定` 的最后一项真机证据  
> 说明：所有真机项目在实际执行和取得证据前均保持“待验”，本记录不预签署结论。

## 1. 固定验收对象

| 项目 | 记录 |
| --- | --- |
| 验收设备 | iPhone 14（设备调试标识：`soda`） |
| iOS 版本 | 26.6.2 |
| 微信版本 | 8.0.78 |
| 基础库版本 | 3.17.3 [1649] |
| 小程序环境 | 微信开发者工具当前正式构建；预览复现问题，现转入真机调试 |
| 项目配置 | `project.config.json` |
| 小程序根目录 | `build/production-miniprogram/` |
| 验收起始基线 | `70f8bbd50d8fbb76e83dfeb5687e00404c2909bf` |
| 客户端修复提交 | `26c6999d80e65f5f87b1be3eafc0b1f724cba56c`（首次新地址显式 `play()`、保留播放器错误细节、增加 iOS 风格回归） |
| 工作分支 | `codex/peter-publish-2026-09-22` |
| 正式产物文件数 | 109 |
| 正式产物字节数 | 582,133 |
| 正式产物 SHA-256 | `fc7959d828ba53524fe3dab2c7e1a8c68e3f94ea6a9d1071e97f74f4da0a0e93` |
| 构建可复现 | 是；`npm run build:verify` 通过 |
| 隔离审计 | 是；`npm run release:audit` 全部通过 |
| 验收人 | 待填写 |
| 网络 | Wi-Fi |

本地未定稿品牌草稿不属于小程序运行代码，也不作为 X-02 验收对象或本次提交内容。

## 2. 测试前准备

1. 在微信开发者工具打开仓库根目录，确认使用 `project.config.json`；
2. 重新编译，确认进入正式模式且书目只有 Peter Rabbit；
3. 使用“预览”生成二维码，由本次 iPhone 微信扫码进入；
4. 记录 iPhone 型号、iOS 版本、微信版本和网络；
5. 清除旧的 `tingyue.product.v1`，从空白正式状态开始；
6. 在每项操作前后保存截图或短视频，文件统一放在 `artifacts/device/x02-iphone-2026-09-24/`。

不得使用 Demo 模式、浏览器预览或模拟器结果替代本次真机证据。

## 3. 真机验收矩阵

### 3.1 诊断记录

| 时间 | 环境 | 操作 | 实际结果 | 当前判断 |
| --- | --- | --- | --- | --- |
| 2026-09-24 | iPhone 微信扫码预览 | 进入 Peter Rabbit 播放页并点击播放 | 始终停在 `00:00`，没有声音，也没有页面错误提示 | `D-01 BLOCKED`；进入真机调试取证 |
| 2026-09-24 | iPhone 自动真机调试 | 重试页面播放并读取页面状态 | `pages/player/index` 已进入 `playbackStatus:"error"`；地址为 Archive.org MP3 | 页面事件已触发，不是按钮未响应 |
| 2026-09-24 | iPhone 自动真机调试 | 直接对 `BackgroundAudioManager` 赋同一 `src` 并调用 `play()` | 仍触发错误；平台事件只返回 `src`，没有稳定错误码；播放器属性随后为空 | 显式 `play()` 不是充分修复；运行时资源交付仍不可用 |
| 2026-09-24 | 开发网络 | 对同一 Archive.org MP3 发起 HTTPS 连接 | 连接超时 | 只证明本次网络不可达，不外推为所有网络永久不可用 |
| 2026-09-24 | 代码与自动验证 | 首次新地址显式 `play()`，扩展错误诊断，使用不自动播放的 manager mock 回归 | `npm test` 90/90；双构建可复现；发布隔离审计通过 | 客户端已消除模拟器隐式自动播放假设；真机矩阵仍由 B 音频交付阻断 |

### 3.2 正式验收项

| ID | 操作 | 预期结果 | 实际结果 | 结论 | 证据文件 |
| --- | --- | --- | --- | --- | --- |
| D-01 | 从头播放至少 10 秒，切到 Home，再返回播放器 | 音频继续；篇目、位置、倍速和播放状态一致 | 待验 | 待验 | 待填写 |
| D-02 | 播放至少 10 秒后用户主动暂停，切页再返回 | 保持暂停，不自动恢复 | 待验 | 待验 | 待填写 |
| D-03 | 正常播放至少 10 秒后把微信切到后台 30 秒，再返回 | 后台音频连续；位置和 LISTENING 增量合理，无重复累计 | 待验 | 待验 | 待填写 |
| D-04 | 正常播放后锁屏 30 秒，通过锁屏控件暂停 10 秒再恢复 | 锁屏播放与系统控件一致；暂停期间不累计 | 待验 | 待验 | 待填写 |
| D-05 | 播放形成可信断点后结束小程序进程，再重新进入同一内容 | 页面停在可信断点且保持暂停；点击后从断点继续 | 待验 | 待验 | 待填写 |
| D-06 | 从前段直接拖到最后几秒并等待结束 | 不标记完成；重启仍回到拖动前的可信断点 | 待验 | 待验 | 待填写 |
| D-07 | 播放中打开其他音频或视频应用，抢占系统音频，再返回微信 | 听阅自动暂停且不自行恢复；状态、断点和计时正确 | 待验 | 待验 | 待填写 |
| D-08 | 在 0.75×、1.25×、2× 各播放一段 | LISTENING 按实际经过时间增长；覆盖率按音频区间增长 | 待验 | 待验 | 待填写 |
| D-09 | 不拖动，从头持续听到自然结束 | 只有唯一覆盖率至少 90% 且尾部 3 秒已听时才标记完成 | 待验 | 待验 | 待填写 |

## 4. 存储抽查

完成 D-03～D-09 后，在开发者工具 Storage 中抽查 `tingyue.product.v1.progress['peter-rabbit-01']`：

| 检查项 | 预期 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| `checkpointSeconds` | 只在连续正常播放满 5 秒后推进 | 待验 | 待验 |
| `listenedRanges` | 不跨越拖动区间；重复区间合并 | 待验 | 待验 |
| `completed` | 仅自然结束、覆盖率 ≥90%、尾部 3 秒已听时为 `true` | 待验 | 待验 |
| `schemaVersion` | `2` | 待验 | 待验 |
| `contentVersion` | 与当前内容版本一致 | 待验 | 待验 |
| 时长累计 | 暂停、缓冲和异常长间隔无明显虚增 | 待验 | 待验 |

## 5. 完成判定

X-02 只有同时满足以下条件才可改为 `DONE`：

- D-01～D-09 全部执行并有可定位的证据；
- 存储抽查全部通过；
- 没有会导致虚假完成、错误断点或时长虚增的阻断问题；
- 记录真实设备、系统、微信版本、验收人和日期；
- 验收人根据实际结果签署，而不是由自动化或 AI 代签。

## 6. 结论与签署

| 结论项 | 记录 |
| --- | --- |
| 阻断问题 | B 尚未提供国内目标网络可访问的正式音频对象存储/CDN地址，也未完成对应微信合法域名配置；当前 Archive.org 运行时地址在本次 iPhone/开发网络中不可播放 |
| 非阻断问题 | 待填写 |
| X-02 是否可标记 `DONE` | 否；保持 `BLOCKED_B_AUDIO_DELIVERY`，待 B 解阻后重新执行 D-01～D-09 |
| 验收人签署 | 待填写 |
| 签署日期 | 待填写 |
