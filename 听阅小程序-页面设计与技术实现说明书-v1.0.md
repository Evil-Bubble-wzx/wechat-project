# 听阅小程序 · 页面设计与技术实现说明书

> 版本：v1.0 ｜ 2026-09-16
> 配套：`听阅小程序-技术架构与需求总览-v1.2.md`（需求源）／`听阅功能-开发任务清单.md`（排期）
> 范围：数字闭环全拉通 ｜ 目标年龄 4–12 岁，先覆盖低龄
> 本文回答五件事：**用什么素材 · 总共有哪些功能 · 每个功能放哪个页面 · 每个页面长什么样 · 技术上怎么实现**

---

# 0. 试点素材（已指定）

## 0.1 指定：《The Tale of Peter Rabbit》

**Beatrix Potter《彼得兔的故事》，LibriVox 单篇 5.4 分钟。**

LibriVox 实测扫描（2026-09-16，`tools/librivox_scan_short.py`，扫描 wilde / andersen / aesop / potter / grimm / perrault / kipling / carroll / sewell / baum / grahame 共 404 条 4–15 分钟单章）：

| 分钟 | 单篇 | 出自 | 作者 |
|---|---|---|---|
| 4.1 | The Mouse, the Bird and the Sausage | Grimm's Fairy Tales (一格音节改写) | grimm |
| 4.2 | The Cat Who Married a Mouse | Grimm's Fairy Tales (一格音节改写) | grimm |
| **4.3** | **The Tale of Tom Kitten** | **The Tale of Peter Rabbit and Others** | **potter** |
| 4.7 | The Town Musicans of Bremen | Grimm's Fairy Tales (一格音节改写) | grimm |
| 4.8 | The Tale of Jeremy Fisher | Collection of Beatrix Potter Stories | potter |
| 5.2 | The Tale of Two Bad Mice | Collection of Beatrix Potter Stories | potter |
| **5.4** | **★ The Tale of Peter Rabbit** | **The Tale of Peter Rabbit and Others** | **potter** |

**为什么不选绝对最短的 4.1–4.3 分钟**：那批里最短的几个是诗歌（Kipling / Carroll，共 30+ 条 4.0–4.3 分钟）或长篇残章（Alice 单章、Black Beauty 单章），不能作为独立篇目。剩下的儿童散文里，4.1–4.7 分钟那批是 **Grimm 一格音节改写本**——文字被人为压缩到单音节，语法破碎、文学性差，且**没有可用的公版插图**。

**Peter Rabbit 多出的 1 分钟换来三件事**：

1. **原版插图确定可用** —— Beatrix Potter 1902 年原版插图，作者 1943 年去世，美国已进入公有领域，中国"终身+50 年"同样过期。这直接满足你选的"公版原版插图"路线，是唯一能立刻拿到高质量配图的篇目。
2. **具象名词密度极高** —— rabbit / garden / lettuce / radish / parsley / cabbage / spade / wheelbarrow / net / gooseberry / coat / shoes / pie / milk / bread / blackberry / churn / watering-can / fir-tree / hay / wall，几乎每一句都有可配图的名词。这是验证"图—词绑定"的最佳样本。
3. **冷启动资产** —— 家长一眼认得，第一批内测家庭不需要被教育"这是什么"。

## 0.2 实测数据

| 项 | 值 |
|---|---|
| 时长 | **5.4 分钟（324 秒）** |
| 32kbps 单声道体积 | **约 1.3 MB** |
| 64kbps 体积 | 约 2.6 MB |
| 版权 | 文本 1902 年，进入公有领域；LibriVox 录音 CC0 |
| **AR / ATOS** | **4.2**（Lexile 660L，AR Quiz 15796，0.5 points）— 已查证 |
| 词数 | 约 1,100 词（待 Gutenberg 原文核对，本环境 gutenberg.org 不可达） |

体积 1.3MB 意味着：整篇常驻缓存毫无压力，20 篇同量级仅 26MB。

## 0.3 ⚠️ 一个必须同时改的规则：出图判定

**问题**：Peter Rabbit 的 AR = **4.2**，而 R7 的规则是「AR < 2 出图片」。按原规则，这篇**一张图都不会出**——而它恰恰是我们插图资源最好的一篇。

**根因**：`AR < 2` 当初是用来近似"低龄"的**代理指标**，不是目的本身。**图片的存在理由是使用者的年龄与阅读阶段，不是文本的 AR 值。** 一个 5 岁孩子听 Peter Rabbit，和一个 5 岁孩子听一格音节版 Grimm，需要图片的程度是一样的。

**改法**（建议，需你确认）：

```ts
// 原规则
level < 2 && vocab[lemma].img  →  出图

// 建议改为
showImage = (ageBand === 'low' || level < 2) && vocab[lemma].img
```

即：**低龄段一律出图，高阶篇目按 AR<2 出图**。`ageBand` 由用户在首次进入时选一次（或由 TEST AGAIN 定级结果推导），存本地。

这样 Peter Rabbit 归入低龄段，图片正常触发；等到做 AR 4–5 的大龄篇目时自动关闭图片，只给文字释义。

## 0.4 备选（若你需要真正的 AR<2 篇目）

若你坚持首篇必须 AR<2（好处是能立刻验证低阶配图链路），改用：

- **《The Cat Who Married a Mouse》** 4.2 分钟，Grimm 一格音节改写，难度约 AR 1.5–2.0
  - 代价：无公版插图可用，配图只能走**开源图标库兜底**；文本质量差
- **《The Tale of Tom Kitten》** 4.3 分钟，Beatrix Potter，有原版插图
  - 代价：难度与 Peter Rabbit 相当（AR 3.5–4.0），同样不触发 AR<2；知名度低很多

**我的建议**：首篇用 Peter Rabbit，并采用 §0.3 的新出图规则。等低阶篇目铺量时，再单独验证 AR<2 那一档。

---

# 1. 总共有什么功能

## 1.1 功能总表（F1–F25）

| 编号 | 功能 | 所在页面 | 优先级 |
|---|---|---|---|
| F22 | 底部 Tab 容器（Home / Recent / Me） | 全局 | P0 |
| F1 | 首页篇目列表，按难度排序 | 首页 | P0 |
| F16 | 搜索（Title / Series / Author / No） | 首页 | P0 |
| F24 | F/NF 分流（虚构 / 非虚构） | 首页 | P0 |
| F17 | 篇目详情页 | 详情页 | P0 |
| F17.1 | 元数据（No. / Words / Lv / F-NF / Pages） | 详情页 | P0 |
| F17.2 | 篇目三态（未开始 / 进行中 / 已完成） | 详情页 · 列表 | P0 |
| F17.3 | 双 CTA（Read for Me / Take Quiz） | 详情页 | P0 |
| F17.4 | SERIES 同系列列表 | 详情页 | P0 |
| F2 | 音频在线播放 | 播放器 | P0 |
| F3 | 后台播放 + 锁屏控制 | 播放器 | P0 |
| F18 | 播放器完整控件（循环三态 / 倍速 / 播放列表） | 播放器 | P0 |
| F4 | 连续播放 | 播放器 | P0 |
| F5 | 句级字幕，5–7 行窗口，默认关 | 播放器 | P0 |
| F6 | 点词查义（音标/词性/中英释义/原句） | 播放器 · 词卡 | P0 |
| F7 | 生词本 | 词卡 · 我的 | P0 |
| **R7** | **AR<2（或低龄段）出图片** | 词卡 | P0 |
| F19 | 最近播放 | Recent Tab | P0 |
| F10 | 阅读进度同步 + 续听 | 全模块 | P0 |
| F8 | Quiz（5–8 题，四层题型） | 答题页 · 结果页 | P0 |
| F9 | 家长报告（STATS 块 + Quiz 列表） | Quizzes 页 | P0 |
| F20 | 排行榜（完成篇数 + 累计时长） | Rankings 页 | P0 |
| F21 | 个人中心（累计三件套 + 菜单） | Me Tab | P0 |
| F23 | 收藏 / 点赞 / 笔记 | 详情页 | P1 |
| F25 | 定级测试 TEST AGAIN | 我的 | P1 |
| F11 | 离线播放 | 播放器 | P1 |
| F12 | 词级卡拉 OK 高亮 | 播放器 | P1 |
| F13 | 跟读打分 | 播放器 | P1 |
| F14 | 按水平推荐篇目 | 首页 | P1 |
| F15 | 打卡与连续天数 | 我的 | P1 |

**合计 26 项功能，其中 P0 20 项。**

## 1.2 明确不做

物理借阅（Call Slips 四态）· 借阅卡（Library N / 有效期）· 线下馆与营业时间 · 本馆维度 · 外教人工点评 · 纸质物流。

## 1.3 待定（先留接口不实现）

社群感（「N 人已参与」横幅 + Congratulations 学员轮播）· 笔记 · 支付记录 / 券包 / 邀请好友（取决于定价模型）。

---

# 2. 信息架构：每个功能放在哪里

```
Tab 容器（F22）
│
├─ Home 首页 ─────────────────────────────────────────
│   ├─ 搜索框 ................................ F16
│   ├─ 分类入口（Find Books / NF / Borrowed×） .. F24
│   ├─ 篇目列表（难度排序 + 三态角标） ......... F1 · F17.2
│   └─ 点击进入 → 详情页
│
├─ Recent 最近播放 ───────────────────────────────────
│   └─ 时间倒序列表 ............................ F19
│
└─ Me 我的 ───────────────────────────────────────────
    ├─ 头部：头像 / 级别 / 累计三件套 ........... F21
    ├─ 入口：Quizzes ........................ → 报告页 F9
    ├─ 入口：Rankings ....................... → 排行页 F20
    ├─ 入口：Favorite Books ................. → 收藏列表 F23
    ├─ 入口：Reading Progress ............... → 进度明细 F10
    └─ TEST AGAIN ........................... F25（P1）

详情页 F17 ──┬─ 元数据四件套 ...................... F17.1
             ├─ 三态 ............................. F17.2
             ├─ Read for Me ──→ 播放器
             ├─ Take Quiz ────→ 答题页
             ├─ 收藏 / 点赞 / 笔记 ............... F23
             └─ SERIES 同系列 .................... F17.4

播放器 ──────┬─ 播放 / 暂停 / seek ............... F2
             ├─ 后台 + 锁屏 ..................... F3
             ├─ 循环三态 / 倍速 / 播放列表 ...... F18
             ├─ 连续播放 ........................ F4
             ├─ 字幕（默认关） .................. F5
             └─ 点词 ──→ 词卡弹层 ............... F6 · F7 · R7

答题页 F8 ──→ 结果页（得分 + 升降级建议）──→ 回详情页
报告页 F9 ──→ STATS 块 + Quiz 列表
排行页 F20 ─→ 7 日榜 / 总榜 + 自己排名
```

**为什么 Quizzes 和 Rankings 放在 Me 里而不是独立 Tab**：照抄 MegaWords 的三 Tab 克制结构。它们是从"我的进度"派生出来的视图，不是并列的内容域。

---

# 3. 每个页面怎么设计

## 3.1 全局：Tab 容器（F22）

```
┌────────────────────────────────┐
│                                │
│         页面内容区              │
│                                │
├────────────────────────────────┤
│   首页      │  最近  │   我的   │  ← 三个 Tab，图标 + 文字
└────────────────────────────────┘
```

- 用 `app.json` 的 `tabBar` 原生实现，不要用自定义组件（原生 tabBar 性能与适配最好）
- 三个 Tab 的页面在切后台播放时**不得销毁播放状态** —— 播放状态放在全局 `BackgroundAudioManager` + 全局 store，不挂在页面上

## 3.2 首页 Home（F1 / F16 / F24 / F17.2）

```
┌──────────────────────────────────────┐
│ 🔍 搜索书名 / 系列 / 作者 / 编号        │  ← F16
├──────────────────────────────────────┤
│ [全部] [虚构 Fiction] [非虚构 NF]      │  ← F24 分流
├──────────────────────────────────────┤
│ 排序：难度 ↑            [筛选 ▾]       │
├──────────────────────────────────────┤
│ ┌────┐  The Tale of Peter Rabbit     │
│ │封面│  Beatrix Potter              │
│ │    │  No.1024 · Lv 4.2 · 5.4min   │
│ └────┘  NF? Fiction · 1100 词   ✅已完成│  ← F17.2 三态角标
│ ────────────────────────────────────│
│ ┌────┐  The Tale of Tom Kitten       │
│ │封面│  Beatrix Potter              │
│ │    │  No.1025 · Lv 3.8 · 4.3min   │
│ └────┘  Fiction · 980 词      ▶︎进行中 │
│ ────────────────────────────────────│
│ ┌────┐  The Cat Who Married a Mouse  │
│ │封面│  Grimm                      │
│ │    │  No.2031 · Lv 1.8 · 4.2min   │
│ └────┘  Fiction · 620 词       未开始 │
└──────────────────────────────────────┘
```

| 元素 | 说明 |
|---|---|
| 搜索 | 防抖 300ms，本地已有数据先过滤，未命中再打服务端 |
| F/NF 分流 | 顶部三档切换，纯前端过滤（`pieces.fn`） |
| 三态角标 | 未开始（无）／进行中（进度环）／已完成（✅ + 得分） |
| 排序 | 默认按 Lv 升序（低龄优先）；可切时长、最近更新 |
| 分页 | 下拉触底加载，每页 20 条 |

**数据**：`GET /books?fn=&sort=level&q=&page=`
**埋点**：列表曝光、搜索词、分类切换、点击进入

## 3.3 篇目详情页（F17）

```
┌──────────────────────────────────────┐
│  ←                              ⋯    │
│  ┌──────────┐                        │
│  │          │  The Tale of           │
│  │   封面   │  Peter Rabbit          │
│  │          │  By Beatrix Potter     │
│  └──────────┘  Lv 4.2    [✅ 已完成]  │  ← F17.2
├──────────────────────────────────────┤
│  No.1024   Words 1100                │  ← F17.1
│  Fiction   Pages 56      5.4 min     │
├──────────────────────────────────────┤
│  📚书架   ♡收藏   👍点赞   📝笔记   ＋加入│  ← F23（P1）
├──────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  │
│  │  🎧          │  │  📝          │  │  ← F17.3 双 CTA
│  │ Read for Me  │  │  Take Quiz   │  │     并排等宽
│  └──────────────┘  └──────────────┘  │
├──────────────────────────────────────┤
│  SERIES (23)                      →  │  ← F17.4
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐            │
│  │7-1│ │7-2│ │7-3│ │7-4│  横向滚动   │
│  └───┘ └───┘ └───┘ └───┘            │
└──────────────────────────────────────┘
```

**关键设计点**

- **双 CTA 并排等宽** —— 照抄 MegaWords。这是"先听后测"的产品承诺，不能做成主次关系
- **Take Quiz 在未听完时置灰但可见** —— 让它可见是为了制造"我要去听"的动机，置灰是为了不让人跳过听直接测。点击时提示"先听一遍效果更好"
- **三态显示在标题右侧**，不用小字——这是用户回来看"我读过没"的第一眼信息
- **SERIES 横向滚动**，每项显示封面 + 编号 + 三态，点击切到该篇详情
- ⚠️ `series_id` 必须在数据模型第一版就有，否则 SERIES 列表要返工

**数据**：`GET /books/:id/pieces` + `GET /pieces/:id`（含我的状态）
**埋点**：详情页曝光、两个 CTA 点击、收藏/点赞、SERIES 点击

## 3.4 播放器页（F2 / F3 / F4 / F5 / F18）

```
┌──────────────────────────────────────┐
│  ←    7-03 Peter Rabbit          ⋯   │
│                                      │
│         ┌────────────────┐           │
│         │                │           │
│         │     大封面      │           │
│         │                │           │
│         └────────────────┘           │
│                                      │
│   [🔂单曲] [🔁顺序] [📃列表]  1.0x     │  ← F18 循环三态 + 倍速
│                                      │
│  ┌────────────────────────────────┐  │
│  │ Every afternoon the children   │  │  ← 次级灰（前 2 句）
│  │ went to play in the garden.    │  │
│  │ ▸ They ran about the garden...  │  │  ← 前 2 句
│  │ ▸ ▸ The garden was large and    │  │  ← 当前句：高亮 + 放大
│  │     lovely, with flowers...     │  │
│  │ and the birds sat in the trees │  │  ← 后 4 句
│  │ and sang so sweetly...         │  │
│  │ ...                            │  │
│  └────────────────────────────────┘  │
│                    [字幕 开/关]        │  ← F5 默认关
│                                      │
│  02:13 ──────●────────────── 05:24   │  ← 秒级进度，可拖
│                                      │
│    ⏮         ⏸           ⏭      📃   │  ← F2 / F18
└──────────────────────────────────────┘
```

| 元素 | 实现要点 |
|---|---|
| 大封面 | 也是锁屏封面图（同一张 URL），必须 ≥300×300 |
| 循环三态 | 单曲 / 顺序 / 列表循环。自己维护队列，`onEnded` 决定下一首 |
| 倍速 | `playbackRate`，取值 0.5 / 0.8 / 1.0 / 1.25 / 1.5 / 2.0，点击循环切换 |
| 字幕 5–7 行 | **渲染窗口** `cues.slice(i-2, i+5)`，不是样式问题。当前句高亮放大，前 2 句次级灰，后 4 句正常 |
| 切句 | **直接换内容，不做滚动动画** —— 句子长短不一，动画必闪 |
| 进度条 | 显示秒级（`02:13`）；拖拽 seek 时先 `pause` 再 `seek` 再 `play`，避免 iOS 卡顿 |
| 锁屏 | `setMeta({ title, singer, coverImgUrl, episodeName })` —— **不设 iOS 锁屏显示不全** |

**字幕开关默认关的理由**：MegaWords 的方法论是"先听后读，不急着认字"，常亮字幕与核心理念冲突。做成开关，默认关，让家长决定。

**埋点**：播放 / 暂停 / seek / 倍速 / 循环切换 / 字幕开关 / 完成

## 3.5 词卡弹层（F6 / F7 / R7）

点词后从底部弹出，音频**自动暂停**。

```
┌──────────────────────────────────────┐
│  ╭────────────────────────────────╮  │
│  │  ┌────────┐                    │  │
│  │  │        │  garden            │  │
│  │  │  插图   │  /ˈɡɑːrdn/        │  │  ← R7：低龄段或 AR<2 出图
│  │  │        │  n. 花园           │  │     无图时 → 只显示文字
│  │  └────────┘                    │  │
│  │                                │  │
│  │  a piece of ground where       │  │  ← 英文释义（限 2000 基础词）
│  │  flowers and vegetables grow   │  │
│  │                                │  │
│  │  ▸ 原句：                       │  │
│  │  "They ran about the garden"    │  │  ← 书中原句，当前词高亮
│  │                                │  │
│  │  [🔊 发音]        [＋ 加入生词本] │  │  ← F7
│  ╰────────────────────────────────╯  │
└──────────────────────────────────────┘
```

| 元素 | 规则 |
|---|---|
| 触发 | **长按**单词（单击只做高亮 + 发音，避免误触） |
| 自动暂停 | 触发即 `pause()`；关闭卡片后**手动继续**，不自动恢复 |
| 插图 | `(ageBand === 'low' \|\| level < 2) && vocab[lemma].img` → 出图；否则只文字 |
| 无图回退 | 动词、抽象词、专名一律文字释义。**图片是增强，不是必需** |
| 英文释义 | 用词必须落在 2000 基础词表内，否则释义比原词还难 |
| 词形还原 | children→child、ran→run。**不还原则查不到** |
| 多义词 | 只给一个义项。生成时必须**把原句作为上下文喂给 LLM** |
| 专有名词 | 标记 `type: 'proper'`，跳过不翻译（Peter Rabbit 里 Peter / McGregor 都不译） |

**埋点**：点词（含 lemma）、发音、加入生词本

## 3.6 Recent 最近播放（F19）

```
┌──────────────────────────────────────┐
│  最近播放                      147 本  │
├──────────────────────────────────────┤
│ ┌───┐ Red Rocket Readers: Early 3    │
│ │封 │ #22 About lighthouses          │
│ │面 │ No.5578 · Lv 1.4 · 165 词      │
│ └───┘                    2026-09-16  │
│ ┌───┐ Oxford Reading Tree 7          │
│ │封 │ 7-03 The Broken Roof           │
│ │面 │ No.201534 · Lv 2.4 · 1016 词   │
│ └───┘                    2026-09-15  │
└──────────────────────────────────────┘
```

时间倒序，去重（同一篇只显示最近一次），点击进入详情页。分页加载。

## 3.7 Quiz 答题页（F8）

```
┌──────────────────────────────────────┐
│  ←              3 / 6         ✕     │  ← 进度指示
├──────────────────────────────────────┤
│                                      │
│  Why did the giant build a wall      │
│  around his garden?                  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ A. To keep out the children    │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ B. To keep out the north wind  │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ C. To grow more flowers        │  │
│  └────────────────────────────────┘  │
│                                      │
│              [ 下一题 ]               │
└──────────────────────────────────────┘
```

| 项 | 规则 |
|---|---|
| 题量 | 每篇 5–8 题 |
| 题型四层 | 字面 → 推断 → 主旨 → 词汇语境。**每层至少 1 题** |
| 交互 | 一题一屏，选中即高亮，**不可回看**（避免用排除法刷题） |
| 中断恢复 | 中途退出保存草稿，7 天内可续答 |
| 提交 | 最后一道答完自动 `POST /quiz/submit` |

## 3.8 Quiz 结果页（F8）

```
┌──────────────────────────────────────┐
│            🎉                        │
│          5 / 6  正确                 │
│                                      │
│         正确率 83%                    │
│                                      │
│  ┌────────────────────────────────┐  │
│  │  建议：保持当前级别              │  │  ← ≥85% 建议升级
│  │  当前 Lv 4.2                    │  │    <60% 建议降级
│  └────────────────────────────────┘  │
│                                      │
│  [ 查看解析 ]      [ 再做一次 ]       │
│  [ 回到详情页 ]                       │
└──────────────────────────────────────┘
```

- 判定：≥85% 建议升级、<60% 建议降级、其余保持
- **不做"不及格"的负面表达** —— 低于 60% 说"再听一遍会有新发现"，不要说"未通过"
- 结果回写 `quiz_score` / `quiz_passed`，篇目状态 → 已完成

## 3.9 报告页 / My Quizzes（F9）

```
┌──────────────────────────────────────┐
│  ←   My Quizzes                      │
├──────────────────────────────────────┤
│  2025-09-15  ~  2026-09-16      [▾]  │
├──────────────────────────────────────┤
│  STATS                通过 / 全部     │
│  ─────────────────────────────────  │
│  Total Pieces        140  / 142      │
│  Total Words      101,657 / 102,116  │
│  Early Correct%     79.0% / 79.0%    │  ← 关键
│  Early Level          1.7 / 1.7      │
│  Rct Correct%       87.7% / 87.7%    │  ← 关键
│  Rct Level            2.2 / 2.2      │
│  Avg Correct%       85.9% / 85.3%    │
│  Avg Level            2.0 / 2.0      │
│  Fiction%           90.7% / 90.8%    │
│  Total Score         86.6 / 87.4     │
├──────────────────────────────────────┤
│  当前年级 4 年级 · 阅读级别 2.1        │
├──────────────────────────────────────┤
│  🔍 搜索                              │
│  142 QUIZZES              Date Taken ▾│
│  ─────────────────────────────────  │
│  #22 About lighthouses               │
│  Pam Holden · +165                   │
│  Correct 100% · Lv 1.4 · NF  09-16   │
└──────────────────────────────────────┘
```

⚠️ **Early vs Recent 这组对比是报告的价值核心，不能砍。** 其余字段都是"数量"，只有这一组让家长看见"进步"。MegaWords 把它做成 PASSED / ALL 双列，我们至少保留 Early / Recent / Avg 三行。

**时间切片定义**：Early = 该用户最早 1/3 的已完成篇目；Recent = 最近 1/3。样本不足 6 篇时隐藏这两行，避免噪音。

## 3.10 排行榜（F20）

```
┌──────────────────────────────────────┐
│  ←   Rankings          [7日榜▾]      │
│                        7日 / 总榜     │
├──────────────────────────────────────┤
│  2026.09.10 - 2026.09.16        [⛭] │
├──────────────────────────────────────┤
│  🥇 Howard ShiRuHong        6 篇 42m │
│  🥈 Gloria ZhouChenGuo      5 篇 38m │
│  🥉 Neo                     4 篇 51m │
│   4  Michael ZhaoHanYi      4 篇 30m │
│   5  Vincent                3 篇 44m │
│   6  Summer                 3 篇 21m │
│   7  Monday                 2 篇 35m │
├──────────────────────────────────────┤
│  416  xixi/xixi             1 篇 12m │  ← 自己，固定底部
└──────────────────────────────────────┘
```

| 项 | 规则 |
|---|---|
| 主指标 | 完成篇数（降序） |
| 副指标 | 累计时长（同分时的第二排序键） |
| 周期 | 7 日榜 / 总榜。**无"本馆"维度**（我们没有线下） |
| **防挂机** | 只统计 `onTimeUpdate` 真实推进的秒数；单日单篇计入上限 ≤ `duration_sec × 1.2` |
| 冷启动降级 | 活跃用户 < 50 时只显示"自己 vs 自己"（本周 vs 上周 + 连续天数），达标后开放全站榜 |
| 隐私 | 昵称默认截断脱敏（如 `Howard S***`），头像默认不显示；设置里可关闭上榜 |

⚠️ **防挂机不是可选项。** 时长榜一旦被刷，激励作用直接反向，还会诱导错误行为。

## 3.11 我的 Me（F21）

```
┌──────────────────────────────────────┐
│  ⚙                              ▣    │
│  ┌───┐                               │
│  │头像│  xixi/xixi (大勇士)           │
│  └───┘  阅读级别 2.1   [TEST AGAIN]   │  ← F25（P1）
├──────────────────────────────────────┤
│   140         101,657      21:30:48  │
│   BOOKS        WORDS       LISTENING │  ← 累计三件套
├──────────────────────────────────────┤
│  📊 Quizzes                        → │  → 报告页 F9
│  🏆 Rankings                       → │  → 排行页 F20
│  ♡  Favorite Books                 → │  F23
│  📈 Reading Progress                → │  F10
│  ✉️  Feedbacks                      → │
│  🔔 Messages                        → │
│  ℹ️  About 4.2.0                     │
└──────────────────────────────────────┘
```

**不做**：Call Slips 四态、借阅卡、支付记录、券包、邀请好友（取决于定价）。

---

# 4. 技术路线

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | **原生微信小程序 + TypeScript** | 目标是移植到其他微信小程序，不为跨端提前付成本 |
| 模块形态 | 固定目录 `modules/listen-read/` + Host Adapter | 隔离登录、路由、主题、请求差异 |
| UI | 原生 WXML/WXSS，不引第三方 UI 库 | 页面不多，UI 库会带来体积与适配负担 |
| 状态 | 页面级 `setData` + 全局播放状态单例 | 播放状态不能挂页面（切 Tab 会销毁） |
| 后端 | 统一内容 API，部署用 **微信云开发 CloudBase** | 起步最快；客户端依赖接口契约而非 SDK 细节 |
| 数据库 | 云数据库（文档型） | 量与复杂度都不高 |
| 音频/图片 | **对象存储 + CDN** | 主包仅 2MB，素材必走网络 |
| 本地缓存 | `FileSystemManager` | `setStorage` 仅 10MB，装不下音频 |
| AI 生成 | 服务端**构建期**调用 LLM 生成词表与 Quiz，**运行时绝不调** | 1000 DAU × 30 词/天 = 每天 3 万次调用，成本与延迟都不可接受 |
| 时间戳 | faster-whisper / whisper.cpp 强制对齐 | 句级精度足够，成本近零 |
| 排行榜 | 服务端定时聚合 → 缓存表 | 容忍 5–10 分钟延迟，避免每次进榜全表扫描 |
| CI/CD | `miniprogram-ci` | 上传、预览、提审自动化 |

**为什么不用 Taro / uni-app**：本期明确不追求跨端。引入跨端框架会让 `BackgroundAudioManager`、锁屏控制、文件系统这些平台强相关能力多一层适配，得不偿失。若未来确有跨端需求，再评估——那时 `modules/listen-read/` 的隔离已经把成本降到最低。

---

# 5. 技术实现

## 5.1 目录结构

```
miniprogram/
├─ app.json                      # tabBar、requiredBackgroundModes、分包
├─ modules/listen-read/          # ★ 可整体复制的独立模块
│  ├─ pages/
│  │  ├─ home/                   # 首页（列表 + 搜索 + F/NF）
│  │  ├─ detail/                 # 篇目详情
│  │  ├─ player/                 # 播放器
│  │  ├─ quiz/                   # 答题
│  │  ├─ result/                 # 结果
│  │  ├─ report/                 # 报告
│  │  ├─ ranking/                # 排行榜
│  │  └─ recent/                 # 最近播放
│  ├─ components/
│  │  ├─ word-card/              # 词卡弹层
│  │  ├─ subtitle/               # 字幕 5–7 行
│  │  └─ piece-item/             # 列表项（含三态角标）
│  ├─ core/                      # ★ 零平台依赖，可单测
│  │  ├─ tokenizer.ts
│  │  ├─ subtitle.ts
│  │  ├─ progress.ts
│  │  ├─ quiz.ts
│  │  ├─ report.ts
│  │  └─ types.ts
│  ├─ adapters/                  # 平台能力适配
│  │  ├─ PlayerAdapter.ts        # 接口
│  │  ├─ WxPlayerAdapter.ts      # BackgroundAudioManager 实现
│  │  ├─ StorageAdapter.ts
│  │  ├─ RequestAdapter.ts
│  │  └─ HostAdapter.ts          # ★ 宿主注入
│  ├─ player/                    # 播放内核（队列、循环三态、倍速）
│  ├─ content/                   # 内容访问
│  ├─ cache/                     # 离线缓存与 LRU
│  └─ config.ts                  # apiBase / cdnBase / 功能开关
└─ pages/                        # 宿主自己的页面
```

## 5.2 依赖方向（ESLint 强制）

```
pages → adapters → core        （单向，不可反向）
```

```js
// .eslintrc.js
{
  "overrides": [{
    "files": ["modules/listen-read/core/**/*.ts"],
    "rules": {
      "no-restricted-globals": ["error", "wx", "window", "document", "my", "swan"],
      "no-restricted-imports": ["error", { "patterns": ["../adapters/*", "../pages/*"] }]
    }
  }]
}
```

`core/` 里出现一个 `wx.` 就 lint 失败——这是可移植性能否兑现的唯一硬保障。

## 5.3 播放：第一天就用 BackgroundAudioManager

```ts
// adapters/WxPlayerAdapter.ts
const ctx = wx.getBackgroundAudioManager()

export class WxPlayerAdapter implements PlayerAdapter {
  play(src: string, meta: Meta) {
    ctx.title = meta.title
    ctx.singer = meta.singer
    ctx.coverImgUrl = meta.cover      // 不设 iOS 锁屏显示不全
    ctx.episodeName = meta.episode
    ctx.src = src                     // 在线用 CDN URL，离线用 wxfile:// 路径
    ctx.playbackRate = this.rate      // 0.5 / 0.8 / 1.0 / 1.25 / 1.5 / 2.0
  }
  pause() { ctx.pause() }
  seek(sec: number) { ctx.seek(sec) }
  get currentTime() { return ctx.currentTime }
  onEnded(cb: () => void) { ctx.onEnded(cb) }
  onTimeUpdate(cb: () => void) { ctx.onTimeUpdate(cb) }   // 约 250ms
}
```

⚠️ **不要用 `innerAudioContext` 起步再换** —— 它一切后台就停，后期替换等于重写。

**循环三态与连续播放**（`player/queue.ts`）：

```ts
type LoopMode = 'single' | 'order' | 'list'

function onEnded() {
  switch (mode) {
    case 'single': return play(current)
    case 'order':  return hasNext() ? play(next()) : stop()
    case 'list':   return play(nextWrapAround())
  }
}
```

**进度上报与防挂机**（`core/progress.ts`）：

```ts
// 只累计 onTimeUpdate 真实推进的秒数，且单日单篇设上限
function accumulate(prev: number, now: number, durationSec: number, dayTotal: number) {
  const delta = Math.max(0, now - prev)
  if (delta > 2.0) return 0                        // 跳变（seek/切后台）不计
  const cap = durationSec * 1.2 - dayTotal
  return Math.min(delta, Math.max(0, cap))
}
```

## 5.4 字幕：5–7 行是渲染窗口，不是样式

```ts
// core/subtitle.ts
export function windowAround(cues: Cue[], i: number, before = 2, after = 4) {
  return cues.slice(Math.max(0, i - before), i + after + 1)
    .map((c, k) => ({ ...c, offset: k - Math.min(before, i) }))
}
```

```js
// pages/player/index.js
ctx.onTimeUpdate(() => {
  const i = cues.findIndex(c => ctx.currentTime >= c.start && ctx.currentTime < c.end)
  if (i !== last) {
    last = i
    this.setData({ currentIndex: i, window: windowAround(cues, i) })  // 只 setData 这两项
  }
})
```

- **只 setData 一个 `currentIndex` + 窗口数组**，绝不整列更新
- **切句直接换内容，不做滚动动画**（句子长短不一，动画必闪）
- 词级高亮（F12，P1）需更高精度：`ctx.currentTime` 可随时主动读 → `setInterval(50)` + WXS 更新

## 5.5 分词与点词

```ts
// core/tokenizer.ts
export function tokenize(sentence: string): Token[] {
  return sentence.split(/([A-Za-z']+)/)
    .filter(Boolean)
    .map(s => /[A-Za-z']+/.test(s) ? { t: 'word', v: s } : { t: 'punct', v: s })
}
```

**渲染禁用 `rich-text`** —— 它的 tap 不冒泡，点词功能直接废掉。用 `<text bindtap>` 逐个渲染：

```xml
<text wx:for="{{tokens}}" wx:key="i"
      class="{{item.t === 'word' ? 'w' : 'p'}}"
      data-w="{{item.v}}" bindlongpress="onWord">
  {{item.v}}
</text>
```

**词表预取**：进篇时一次拉本篇全部释义（Peter Rabbit 约 1,100 词，去重后需标注约 300–400 词，约 15–20KB）→ 本地命中零延迟。未命中才走单条请求并回写。

**服务端绝不实时调 LLM**：首次生成入库，之后纯查询。

## 5.6 配图：三级回退（公版原版插图 → 开源图标库 → 文字）

```ts
function resolveImage(lemma: string, piece: Piece, ageBand: AgeBand): string | null {
  if (!(ageBand === 'low' || piece.level < 2)) return null   // 出图判定
  const v = vocab[lemma]
  if (!v || v.type === 'proper') return null                 // 专名不配图
  return v.img ?? ICON_FALLBACK[lemma] ?? null               // 原版插图 → 图标库
}
// 返回 null → 走文字释义
```

| 级别 | 来源 | 覆盖 |
|---|---|---|
| **Tier 1 公版原版插图** | Beatrix Potter 1902 原版插图 | Peter Rabbit 内出现的具象名词，逐个人工绑定 |
| **Tier 2 开源图标库** | 开源图标集（CC0 / 公开许可） | Tier 1 没图的常见具象名词 |
| **Tier 3 文字回退** | — | 动词、抽象词、专名。**这是默认，不是异常** |

**只给 150–300 个高频具象名词配图**，不要试图覆盖全部词汇。

⚠️ **每一张图都必须登记来源与许可状态**（来源 URL、出版物、年份、权利判断）。这是唯一会被追溯的地方，不能凭"网上找的"就上线。

## 5.7 Quiz

```ts
// core/quiz.ts
export function judge(score: number, total: number): 'up' | 'down' | 'keep' {
  const pct = score / total
  if (pct >= 0.85) return 'up'
  if (pct < 0.60) return 'down'
  return 'keep'
}
```

题目在构建期由 LLM 生成（**必须把原文喂进去**），人工抽检 ≥10%，错误率控制在 5% 内。四层题型各至少 1 题。

## 5.8 报告聚合

```ts
// core/report.ts
export function stats(records: QuizRecord[]): ReportStats {
  const done = records.filter(r => r.completed)
  const n = done.length
  const third = Math.floor(n / 3)
  const early = done.slice(0, third)
  const recent = done.slice(-third)
  return {
    totalPieces: n,
    totalWords: sum(done, r => r.wordCount),
    earlyCorrect: pct(early), earlyLevel: avgLevel(early),
    rctCorrect: pct(recent),  rctLevel: avgLevel(recent),
    avgCorrect: pct(done),    avgLevel: avgLevel(done),
    fictionPct: ratio(done, r => r.fn === 'fiction'),
    totalScore: weighted(done),
  }
}
```

样本 < 6 篇时隐藏 Early / Recent 两行。

## 5.9 排行榜聚合（服务端）

```sql
-- 定时任务（如每 10 分钟）写入 ranking_cache
INSERT INTO ranking_cache (period, user_id, nickname, pieces_done, listen_sec, rank, updated_at)
SELECT '7d', user_id, nickname,
       COUNT(*) FILTER (WHERE completed),
       SUM(listen_sec),
       RANK() OVER (ORDER BY COUNT(*) FILTER (WHERE completed) DESC,
                             SUM(listen_sec) DESC),
       NOW()
FROM user_progress
WHERE last_at >= NOW() - INTERVAL '7 days'
GROUP BY user_id, nickname;
```

接口只读缓存表，容忍 5–10 分钟延迟。

## 5.10 数据模型

| 表 | 字段 |
|---|---|
| `books` | id, title, author, year, cover_url, level_tier, **series_id**, status, content_version |
| `pieces` | id, book_id, index, title, audio_url, audio_hash, duration_sec, size_bytes, word_count, pages, level, **fn**, status, content_version |
| `cues` | piece_id, [{start, end, text, zh}] |
| `vocab` | piece_id, {lemma: {ph, pos, zh, en, img, img_source, type}} |
| `quiz` | piece_id, [{q, options, answer, type}] |
| `user_progress` | user_id, piece_id, progress_sec, **listen_sec**, **listen_sec_day**, completed, quiz_score, quiz_passed, last_at |
| `user_vocab` | user_id, lemma, looked_at, count |
| `user_favorite` | user_id, piece_id, created_at |
| `ranking_cache` | period, user_id, nickname, pieces_done, listen_sec, rank, updated_at |
| `user_profile` | user_id, nickname, avatar, **age_band**, level, grade |

`books.id` / `pieces.id` 必须由内容包显式提供且长期稳定，**不能用数组下标或数据库自增 ID**——用户进度依赖它们跨环境迁移。

## 5.11 接口

| 接口 | 说明 |
|---|---|
| `GET /books?fn=&sort=&q=&page=` | 书目/篇目列表（F1 F16 F24） |
| `GET /pieces/:id` | 篇目详情（F17） |
| `GET /pieces/:id/manifest` | **一次下发 cues + vocab + quiz** |
| `POST /quiz/submit` | 提交答案 → 得分与升降级建议 |
| `POST /vocab/batch` | 批量取词（未命中时 LLM 生成并入库） |
| `POST /progress` | 上报进度 / 播放秒数 |
| `GET /report/me?from=&to=` | 报告 STATS 块 |
| `GET /ranking?period=7d` | 排行榜（读缓存） |
| `GET /recent` | 最近播放 |

统一响应：`{ code, data, message, traceId, schemaVersion }`

## 5.12 内容包与导入

```
content/peter-rabbit/
├─ manifest.json
├─ cover.jpg
├─ audio/001.mp3           # 5.4 min，32kbps 单声道，约 1.3MB
└─ images/                 # 公版原版插图 + 图标库
   ├─ rabbit.png
   ├─ garden.png
   └─ ...
```

```json
{
  "schemaVersion": 1,
  "book": {
    "id": "peter-rabbit",
    "title": "The Tale of Peter Rabbit",
    "author": "Beatrix Potter",
    "year": 1902,
    "cover": "cover.jpg",
    "seriesId": "potter-tales",
    "fn": "fiction"
  },
  "pieces": [{
    "id": "peter-rabbit-01",
    "index": 1,
    "title": "The Tale of Peter Rabbit",
    "audio": "audio/001.mp3",
    "durationSec": 324,
    "wordCount": 1100,
    "level": 4.2,
    "fn": "fiction"
  }]
}
```

导入流程：`content-import validate` → 格式校验 → 音频转码 → 计算时长体积 → 上传对象存储 → 生成 CDN URL → 生成字幕/词表/Quiz → 写库 → 输出报告。**新增内容不改前端代码、不发版。**

---

# 6. 排期与验收

## 6.1 排期（对应 `听阅功能-开发任务清单.md` v2）

代码约 **30 人日** + 内容 **5–8 人日**；1 人 7–8 周，2 人约 5 周。

| 周 | 内容 |
|---|---|
| 1 | 地基 + Tab + core + 播放 + **Peter Rabbit 数据跑通** |
| 2 | 首页 + 详情 |
| 3 | 字幕 + 点词 + 最近播放 + 进度 |
| 4 | Quiz |
| 5 | 报告 + 排行 |
| 6 | 我的 + 真机验收 |
| 7–8 | 铺量 + 可选 P1 |

## 6.2 闸门

| 闸门 | 标志 |
|---|---|
| **M1（第 1 周末）** | Peter Rabbit 一篇打通：能播 + 有字幕 + 能点词 + 能出图 |
| **M3（第 4 周末）** | Quiz 可作答并出分 —— 数字闭环的第一个真检验 |
| **M5（第 6 周末）** | iOS + Android 双端后台播放真机验收通过 |

## 6.3 本篇专项验收（Peter Rabbit）

| 项 | 指标 |
|---|---|
| 时间戳 | 随机抽 10 句，对齐误差 ≤ 0.5 秒 |
| 词表 | 抽检 10%，错误率 ≤ 5% |
| 配图 | 具象名词覆盖率 ≥ 60%；**每一张都有来源登记** |
| 专名 | Peter / McGregor / Flopsy 等不翻译、不点词 |
| 字幕 | 跟随误差 ≤ 300ms；播放 5 分钟无闪烁 |
| 首播 | ≤ 1.5s |
| 体积 | 转码后 ≤ 1.5MB |

---

# 7. 本文遗留（需你确认）

1. **§0.3 出图规则改为 `(ageBand === 'low' || level < 2)`** —— 否则 Peter Rabbit（AR 4.2）一张图都不出
2. **插图来源逐张登记** —— Peter Rabbit 用 Beatrix Potter 1902 原版插图，需确认取图渠道（Internet Archive / Gutenberg）并逐张记录权利状态
3. **社群感 / 笔记 / 定价** —— 仍未定，影响"我的"页菜单
4. **`series_id`** —— 第一版数据模型就要有，否则 SERIES 列表返工
