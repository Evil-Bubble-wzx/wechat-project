# 听阅小程序：技术架构与需求总览

> 版本：v1.1 ｜ 修订：2026-09-16
> 目标：复刻 MegaWords 的核心「听阅」体验，纯数字化实现，零版权成本
> 定位：英语阅读学习工具，**音频为配套材料**（不是听书平台——这关系到类目与资质）
> 工程约束：当前只交付一个独立的「听阅」功能，但必须能够低成本移植到其他已开发中的微信小程序；新增书籍和音频不得依赖改代码、重新发版

## 本次修订：重新定义“可移植性”

本文所说的高可移植性，**不是**把整个项目迁移到 H5、App 或 Taro，也不是优先建设一套通用中台。它特指两件事：

1. **功能可移植**：先把听阅功能做成边界清晰的独立业务模块。以后接入另一个已开发一半的小程序时，主要工作应是复制模块、安装依赖、配置入口和适配少量宿主能力，而不是重写播放器、页面和数据逻辑。
2. **内容可扩展**：后续加入一本书或一批音频时，应通过标准目录、清单文件和导入脚本完成；正常新增内容不改前端代码、不改数据库结构，也不要求重新发布小程序。

因此，本期不追求“做一个包治百病的平台”，而是追求：**一个可独立运行、可嵌入已有小程序、可持续加内容的听阅模块**。

---

## 0. 产品定位

| 项 | 决定 |
|---|---|
| 一句话 | 让孩子真正读完一本英文经典，而不是刷完 100 本分级小书 |
| 与 MegaWords 的差异 | 不做纸质借阅、不做线下馆、不做重人工；用 AI 替代人工解读 |
| 与 ABC Reading 的差异 | 不做泛读刷量，做**深度精读单篇** |
| 内容单位 | **「篇」而非「本」**——合集里的单篇、长篇的单章，都是独立单元（见下方说明） |
| 核心体验 | 听整篇（沉浸）→ 看字幕（可选，默认关）→ 点词查义（自动暂停）→ 做 Quiz → 家长看见进步 |

### 关于"内容单位"：它不是地基，是粒度决策

**播放层确实只认一个 URL**，播放器不关心它叫"篇"还是"本"。之所以按「篇」而非「本」组织，是因为另外五处受影响：

| 受影响处 | 单位是「本」 | 单位是「篇」 |
|---|---|---|
| 缓存粒度（硬约束） | 200MB 一本，缓存不了 | 5MB 一篇，可缓存 |
| 数据下发（硬约束） | 整本字幕+词表几十 MB | 一篇几百 KB，秒开 |
| 进度记录 | "读了 0.2 本"，无完成感 | "读完第 7 篇" |
| Quiz | 9 小时的书做 8 道题不合理 | 一篇 8 道题，测得住 |
| 家长报告 | "本月读了 0.3 本" | "读完 6 篇，累计 2.4 小时" |

**实现方式**：`book` 只是分组容器，URL 挂在 `piece` 上，做成两级。成本几乎为零。

**什么时候不重要**：纯在线 + 不记进度 + 不做 Quiz + 不做离线，平铺列表即可。
**什么时候必须定死**：只要做**离线缓存**或**按篇 Quiz**，单位必须先定——下载管理和题目生成都按单位走，事后改要重写。

---

## 1. 需求清单

### 1.1 功能需求

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| F1 | 书目/篇目列表，按难度排序 | P0 | 显示难度等级、时长、是否已缓存 |
| F2 | 音频在线播放（边下边播） | P0 | `BackgroundAudioManager` 直接播 URL |
| F3 | **后台播放 + 锁屏控制** | P0 | 车上/睡前场景刚需；必须设 title/singer/coverImgUrl |
| F4 | 连续播放 | P0 | `onEnded` 自动切下一篇 |
| F5 | 句级字幕同步高亮 | P0 | **默认关闭**，可一键开启 |
| F6 | 点词查义（音标+词性+中英释义+原句） | P0 | **自动暂停**；长按触发，单击发音/高亮 |
| F7 | 生词本 | P0 | 点过的词自动收集 |
| F8 | 每篇 5–8 题 Quiz | P0 | ≥85% 建议升级，<60% 建议降级 |
| F9 | 家长报告 | P0 | 读了几篇、累计时长、正确率趋势 |
| F10 | 阅读进度同步 | P0 | 跨设备、中断续听 |
| F11 | **离线播放** | P1 | 下载到小程序本地文件，上限 200MB |
| F12 | 词级卡拉 OK 高亮 | P1 | 需 50ms 轮询 `currentTime`，配 WXS |
| F13 | 跟读打分 | P1 | |
| F14 | 按水平推荐篇目 | P1 | |
| F15 | 打卡与连续天数 | P1 | |

### 1.2 非功能需求

| 项 | 指标 |
|---|---|
| 音频首播启动 | ≤ 1.5s |
| 字幕同步误差 | ≤ 300ms（句子级） |
| 点词响应 | **0 延迟**（本地预取命中） |
| 离线可用性 | 已缓存篇目 100% 可离线听 |
| 本地占用 | 常驻 ≤ 100MB（LRU 自动清理） |
| 弱网表现 | 音频可边下边播，字幕/词表异步拉取不阻塞播放 |

---

## 2. 技术架构

```
┌─ 宿主小程序 ──────────────────────────────────────┐
│ 登录/用户标识 · 路由入口 · 主题样式 · 云环境配置    │
│                 ↓ 通过 Adapter 注入                │
├─ 听阅功能模块 modules/listen-read ────────────────┤
│ pages 页面 · components 组件 · player 播放内核      │
│ content 内容访问 · progress 进度 · cache 离线缓存   │
│ 模块内只依赖统一接口，不直接耦合宿主业务             │
├─ 内容与业务服务 ──────────────────────────────────┤
│ 内容 API · 词库服务 · 进度同步 · Quiz 与报告        │
├─ 内容导入管道 tools/content-import ───────────────┤
│ 校验 manifest → 转码/上传 → 生成时间戳/词表/题目    │
│ → 写库/发布；新增内容不修改小程序代码               │
├─ 存储与分发 ──────────────────────────────────────┤
│ 对象存储 + CDN（音频/封面）· 数据库（元数据/进度）  │
└───────────────────────────────────────────────────┘
```

### 2.1 模块边界与移植方式

建议把功能放在固定目录 `modules/listen-read/`，不要散落到宿主项目的 `pages`、`utils` 和全局状态中。模块对宿主只暴露少量接口：

```ts
interface ListenReadHostAdapter {
  getUserId(): Promise<string>
  request<T>(options: RequestOptions): Promise<T>
  getTheme(): { primaryColor: string; textColor: string }
  navigateBack(): void
  reportEvent(name: string, data?: Record<string, unknown>): void
}
```

移植到另一个微信小程序时，只需：

1. 复制 `modules/listen-read/`（后续稳定后可改为 Git subtree 或 npm 私有包，但首版不必过度工程化）；
2. 在宿主 `app.json` 注册听阅页面或分包；
3. 实现一个宿主 Adapter，接上现有登录、请求封装、主题和埋点；
4. 配置 API/CDN 地址与功能开关；
5. 完成播放、登录态、返回路径和真机后台播放的回归测试。

**禁止耦合项**：模块不得直接读取宿主全局 store 的内部字段，不得写死另一个项目的用户表，不得依赖宿主页面路径，不得在页面里写死书目和音频 URL。

### 2.2 内容扩展方式

每本书使用同一种内容包结构：

```text
content/<book-id>/
├─ manifest.json       # 书籍元数据、章节顺序、版本号
├─ cover.jpg
└─ audio/
   ├─ 001.mp3
   ├─ 002.mp3
   └─ ...
```

`manifest.json` 示例：

```json
{
  "schemaVersion": 1,
  "book": {
    "id": "the-secret-garden",
    "title": "The Secret Garden",
    "author": "Frances Hodgson Burnett",
    "cover": "cover.jpg",
    "level": "L3"
  },
  "pieces": [
    {
      "id": "chapter-01",
      "index": 1,
      "title": "There Is No One Left",
      "audio": "audio/001.mp3"
    }
  ]
}
```

导入命令只接受内容目录，不要求编辑源码，例如：`content-import validate`、`content-import publish`。发布流程应自动完成：格式校验、音频探测与统一转码、计算时长和体积、上传对象存储、生成 CDN URL、生成或导入字幕/词表/Quiz、写入数据库、输出导入报告。

内容需有 `draft / published / archived` 状态和 `content_version`。客户端只读取已发布内容；修改音频或字幕时提升版本号，缓存按版本失效。这样新增和修订书籍都不需要重新发小程序版本。

**技术选型**

| 层 | 选择 | 理由 |
|---|---|---|
| 前端 | 原生微信小程序 + 独立业务模块 | 当前目标是移植到其他微信小程序；不为跨端迁移提前付出成本 |
| 模块集成 | 固定目录 + Host Adapter + 独立配置 | 隔离登录、路由、主题和请求差异，避免绑定某个宿主项目 |
| 后端 | 统一内容 API；部署可先用 CloudBase | 客户端依赖接口契约而不是 CloudBase SDK 细节，便于复用 |
| 音频 | 云存储 + CDN | 主包仅 2MB，音频必走网络 |
| 本地缓存 | `FileSystemManager`（非 `setStorage`） | 后者仅 10MB，装不下音频 |
| 内容维护 | manifest + 导入脚本 + 管理状态 | 新增书和音频不改代码、不发版，可批量校验与发布 |

---

## 3. 关键技术方案

### 3.1 音频播放：三条链路

| 场景 | 实现 |
|---|---|
| **在线** | `BackgroundAudioManager.src = CDN URL`，边下边播 |
| **离线** | `wx.downloadFile` → `saveFile` → src 用本地 `wxfile://` 路径 |
| **后台** | 必须 `BackgroundAudioManager`（`innerAudioContext` 一切后台即停）；`app.json` 声明 `requiredBackgroundModes: ["audio"]`，并向微信申请接口权限 |

⚠️ 已知风险：**iOS 上 `BackgroundAudioManager` 播本地文件的兼容性有社区反馈**，必须真机实测。

### 3.2 字幕同步

```js
cues = [{ start: 52, end: 84, text: 'You have been my friend.', zh: '你一直是我的朋友。' }]

ctx.onTimeUpdate(() => {                    // 约 250ms
  const i = cues.findIndex(c => ctx.currentTime >= c.start && ctx.currentTime < c.end)
  if (i !== last) { last = i; this.setData({ currentIndex: i, scrollTo: `line-${i}` }) }
})
```

- 词级高亮需更高精度：`ctx.currentTime` 可随时主动读取 → `setInterval(50)` 轮询 + WXS 更新
- **只 setData 当前索引**，绝不整列更新
- 长篇只渲染当前句前后各 N 行

### 3.3 点词查义：预翻译 + 批量预取 + 本地缓存

**分词**（小程序特有坑：`rich-text` 的 tap 不冒泡，禁用）

```js
const tokens = sentence.split(/([A-Za-z']+)/)
  .map(s => s.match(/[A-Za-z']+/) ? { type: 'word', v: s } : { type: 'punct', v: s })
```

**策略**：服务端共享词库 + 进篇时批量预取（一个请求拉本篇全部释义，约 15KB）+ 本地命中零延迟 + 未命中走单条请求并回写。

**服务端绝不能实时调 LLM**：首次生成后入库，之后纯 DB 查询。否则 1000 DAU × 30 词/天 = 每天 3 万次 LLM 调用。

**五个必处理细节**：
1. 词形还原（children→child、ran→run）
2. **释义本身的难度控制**——英文释义用词须落在 2000 基础词内，否则释义比原词还难
3. 多义词只给一个义项——**必须把原句作为上下文喂给 LLM**
4. 专有名词标记跳过（Charlotte / Wilbur）
5. 停用词不响应

### 3.4 Quiz

- 每篇 5–8 题，四层题型：字面 → 推断 → 主旨 → 词汇语境
- 构建期 AI 生成 + 人工抽检
- 判定规则：≥85% 升级、<60% 降级

### 3.5 离线缓存策略

- 只保留**当前篇 + 下一篇**，听完即删，LRU 兜底
- 预下载默认仅 WiFi
- 文件被系统清理 → 检测不存在则重下（需 loading 提示）

### 3.6 部署

开发版 → 体验版 → 提交审核（1–2 天）→ 分阶段发布 → 全量（可回退）。
`miniprogram-ci` 接 CI/CD。主包 ≤2MB、总包 ≤20MB。

---

## 4. 数据模型

| 表 | 字段 |
|---|---|
| `books` | id, title, author, year, pd_status, cover_url, level_tier, status, content_version, published_at |
| `pieces` | id, book_id, index, title, audio_url, audio_hash, duration_sec, size_bytes, word_count, level, status, content_version |
| `cues` | piece_id, [{start, end, text, zh}] |
| `vocab` | piece_id, {word: {lemma, ph, pos, zh, en, type}} |
| `quiz` | piece_id, [{q, options, answer, type}] |
| `user_progress` | user_id, piece_id, progress_sec, completed, quiz_score, last_at |
| `user_vocab` | user_id, word, looked_at, count |

`books` 和 `pieces` 的 ID 必须由内容包显式提供且长期稳定，不能使用数组下标或数据库自动 ID 作为跨环境标识。用户进度依赖这些稳定 ID，才能在不同宿主小程序和不同环境中正确迁移。

---

## 5. 接口设计

| 接口 | 说明 |
|---|---|
| `GET /books` | 书目列表 |
| `GET /books/:id/pieces` | 篇目列表（含时长、体积、难度） |
| `GET /pieces/:id/manifest` | **一次下发本篇 cues + vocab + quiz** |
| `POST /vocab/batch` | 批量取词（未命中时 LLM 生成并入库） |
| `POST /progress` | 上报进度 / Quiz 结果 |

接口返回应带 `schema_version` 和 `content_version`。模块只能通过内容 API 读书目，不能把书单编译进前端代码。若多个宿主小程序需要共享学习记录，服务端增加 `external_user_id + app_id` 身份映射；若暂不共享，则各宿主只需实现各自的 `getUserId()`，听阅模块无需改动。

---

## 6. 内容源与版权

**唯一无枷锁的起点：公版书 + LibriVox。**

- 美国 1930 年及以前出版的作品已入公有领域；按中国"终身+50年"计算同样过期
- LibriVox 录音为 CC0，可自由使用与商用
- 分级：AR 值与 Lexile 值可从公开工具查（AR BookFinder、Lexile Find a Book），**只建索引，不搬题库**
- Quiz：AI 生成，公版原文可自由使用

⚠️ 三条红线：
1. 不用现代出版社的插图版/注释版（有独立权利）
2. 迪士尼化视觉形象有商标保护
3. **翻译权是独立的著作财产权**——保护期内的作品，翻译并展示比展示原文更敏感

---

## 7. 容量与性能：真实数据

以下时长来自 LibriVox API 实测（2026-09-16）。体积按 32kbps 单声道 = 4 KB/s 计算。

| 作品 | 总时长 | 篇/章 | 单篇均时长 | 单篇(32k) | 整部(32k) |
|---|---|---|---|---|---|
| Just So Stories | 3.6 h | 12 | 17.7 min | 4.2 MB | 50 MB |
| Peter Pan | 5.1 h | 17 | 17.9 min | 4.2 MB | 71 MB |
| Treasure Island | 7.5 h | 26 | 17.3 min | 4.1 MB | 106 MB |
| Anne of Green Gables | 10.5 h | 38 | 16.6 min | 3.9 MB | 148 MB |
| The Jungle Book | 5.0 h | 11 | 27.5 min | 6.4 MB | 71 MB |
| The Railway Children | 5.0 h | 14 | 21.5 min | 5.0 MB | 71 MB |
| The Blue Fairy Book | 13.9 h | 37 | 22.5 min | 5.3 MB | 195 MB |
| A Little Princess | 7.6 h | 19 | 24.0 min | 5.6 MB | 107 MB |
| The Secret Garden | 9.1 h | 27 | 20.3 min | 4.8 MB | 129 MB |
| White Fang | 8.5 h | 25 | 20.3 min | 4.8 MB | 119 MB |
| The Wind in the Willows | 6.5 h | 12 | 32.5 min | 7.6 MB | 91 MB |
| The Prince and the Pauper | 6.9 h | 20 | 20.8 min | 4.9 MB | 97 MB |
| The Call of the Wild | 3.4 h | 6 | 33.9 min | 8.0 MB | 48 MB |
| Robinson Crusoe | 13.9 h | 20 | 41.8 min | 9.8 MB | 196 MB |
| Little Women | 19.0 h | 47 | 24.2 min | 5.7 MB | **267 MB** |

**关键结论：**

- 小程序本地文件上限 **200MB** → 除 Little Women、Robinson Crusoe、Blue Fairy Book 外，多数作品整部可离线
- **单篇普遍 4–8MB** → 按「篇」缓存是正确粒度，20 篇约 100–160MB
- 转码建议：**ffmpeg 批量转 32kbps 单声道**，比源文件小一半以上，转一次永久复用
- 流量：32kbps 听 1 小时约 14MB

**LibriVox 已确认可用**：柳林风声、秘密花园、金银岛、丛林之书、绿山墙的安妮、蓝精灵童话集、彼得潘、小妇人、野性的呼唤、白牙、鲁滨逊漂流记、王子与贫儿、原来如此的故事、铁路边的孩子们、小公主、小火柴女孩(安徒生合集)、绒布小兔。
**需人工确认**：Peter Rabbit、Black Beauty、Aesop、Tom Sawyer、绿野仙踪、Grimm、Gulliver（API 标题检索未命中，不代表没有）。

---

## 8. 合规与风险

| 风险 | 说明 | 应对 |
|---|---|---|
| **类目认定** | 以音频为核心可能被归为"音频/听书"而非"教育"，资质要求不同 | 明确"英语阅读学习工具，音频为配套"；不做"电台/听书"栏目 |
| 版权 | 翻译权、音频权、改编权各自独立 | 只用公版；任何现代版本先查权利 |
| K12 学科培训红线 | 双减下学科培训受限 | 定位"阅读素养/工具"，不做提分课、外教课 |
| 效果承诺 | 承诺提分会被打脸且有合规风险 | 只承诺"读了多少、坚持多久" |
| 备案与认证 | 小程序强制 ICP 备案；企业认证 300 元/年 | 提前办 |
| UGC 内容安全 | 如有书评/打卡发言 | 接 `msgSecCheck` |

---

## 9. 部署与运营

- **发布流水线**：开发版 → 体验版 → 审核 → 分阶段发布 → 全量（可回退）
- **触达能力弱**：订阅消息一次授权只发一条 → 通知能力必须建在企微/公众号，不在小程序
- **迭代被审核卡**：文案、价格、开关全放服务端配置，尽量不发版
- **增长入口**：搜一搜 SEO、公众号关联、分享卡片、带参小程序码

---

## 10. 里程碑

| 阶段 | 目标 |
|---|---|
| 第 1 周 | 定义模块目录、Host Adapter、内容 manifest 与 API 契约；用 1 本书跑通导入 |
| 第 2 周 | 完成独立听阅模块的列表、播放、字幕与进度；不得写死宿主依赖 |
| 第 3–4 周 | 点词查义 + Quiz + 家长报告；完成批量内容校验、上传与发布脚本 |
| 第 5 周 | 把同一模块接入一个最小测试宿主，验证复制与 Adapter 集成流程 |
| 第 6 周 | 接入 20 个短篇，完成后台播放、内容版本更新和 10 个家庭内测 |
| 第 7–8 周 | 按判定线决定是否继续；继续则补离线与 P1，并在目标半成品小程序做一次真实移植演练 |

**工程验收线**：

- 在不修改听阅模块业务源码的前提下，能通过 Adapter 接入第二个小程序；
- 新增一本符合规范的书，从放入内容目录到测试环境可见，只需运行导入/发布流程；
- 新增书籍不需要修改前端书单、页面路由或数据库表结构，也不需要重新提交小程序审核；
- 替换某章音频后，`content_version` 更新，旧缓存可被准确识别并刷新；
- 内容导入失败时给出具体到书、章节和字段的错误报告，不产生半发布状态。

**判定线**（提前定死，不靠加功能自欺）：
- 内测家庭 7 天内完成 ≥1 篇 + 1 次 Quiz 比例 ≥50%
- 次日留存 ≥30%，7 日留存 ≥15%
- ≥3 位家长主动问"怎么付费/有没有更多篇"
- ≥1 位家长愿付 99 元/年

---

## 11. 待你决策

1. 开发资源：自研还是外包（决定 4 周还是 3 个月）
2. 目标年龄：低年级（听力优先）还是中高年级（自主阅读）
3. 定价：免费+增值 / 订阅 99–199 元/年 / 按篇买断
4. 首个需要移植到的半成品小程序使用的是原生微信小程序、Taro 还是 uni-app（这决定模块交付形态；若也是原生，优先固定目录 + Adapter）
5. 有没有教育行业资源（老师、家长群）

---

## 附：本文档依据

- `MegaWords小程序竞品调研.md` —— 高价重模式拆解 + PC 端实测
- `ABC Reading与英文Quiz体系调研.md` —— 低价数字化路线 + Quiz 品类空档
- `英文原版阅读小程序起步方案.md` —— 内容源、成本模型、冷启动
- `tools/librivox_check.py` —— LibriVox 音频可用性实测脚本
