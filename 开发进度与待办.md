# 听阅小程序：当前进度与待办

更新：2026-09-24。以实际代码和测试结果为准，当前交付为原生小程序前端及浏览器交互预览，未上线。

## 正式产品阶段状态

- 阶段 0 已于 2026-09-20 启动，核验基线见 `docs/阶段0核验表-v1.0.md`；当前为 `NO-GO`，尚未获准真实收款或正式内容发布。
- 内容权利台账已建立于 `docs/内容权利台账-v1.0.md`；Peter Rabbit 的 C-03 已完成公开证据整理、来源对齐和不明来源素材替换，达到 `READY_FOR_EXTERNAL_REVIEW`，但没有任何作品达到 `APPROVED`，所有内容继续保持内测/待审核状态。
- 当前仓库基线：`npm test` 89/89 通过；正式产物 10 个路由约 559 KiB，Demo 产物 13 个路由约 2447 KiB；双构建 SHA-256 可复现；`node tools/content-check.js` 通过 10 本内容结构校验；两套生成产物的浏览器回归通过，覆盖真实音频可靠进度、X-04 可信边界、X-05 物理隔离、X-06 网络边界与 S-01 本机导入边界。这些自动检查不替代整体人工内容、账号、支付、权利、隐私、真实后端联调和真机验收。
- 单一源码已生成正式/Demo 两套产物；默认 `project.config.json` 只加载正式包，正式包仅保留 Peter Rabbit，并物理排除优惠券、邀请、演示购买、实体借阅、固定验证码、开发缓存键与旧书。Demo 使用独立配置与缓存。微信原生开发版已验证显式进入 Demo、清除后恢复正式模式；体验版/正式版 Android 与 iPhone 仍待验收。
- 阶段 0 下一阻断：项目负责人提供小程序认证与商户绑定证据，确认 Android/iPhone 收费路径、正式商品/售价/退款规则、目标经营地区，并指定内容权利与运营负责人。
- X→B 交接基线已记录于 `docs/X-B后端开发交接文档-v1.md`：客户端继续在 `peter` 开发；后端同事取得本文后自行创建长期平行的 B 分支，在 `backend/` 实施 B-01～B-05，并以契约版本和 B 分支提交 SHA 向 X 提供权威 OpenAPI/Schema。
- `X-06` 客户端网络基础层已完成：默认关闭、无真实域名，包含微信 transport、内存 access token、并发刷新锁、退出竞态清理、超时/取消、GET 限次重试、稳定错误和 16 项专项测试；真实登录与业务接口等待固定 B 契约。
- `S-01` 客户端基础已完成并保持 `PARTIAL_BACKEND_PENDING`：只导出正式进度、生词和本机未验证 Quiz 候选，Demo/权益字段阻断，派生完成与分数不上传；确定性快照、显式同意和 mock 单飞测试通过。真实 API、页面和服务端回执等待 B 契约。

## 最新确认的界面要求

- 主界面只有 Home / Recent / Me；Find Books 与 Borrowed 保留为二级功能入口。
- 主要功能组件使用英文，中文说明与中英释义可保留。
- 首页取消重复的“发现下一本喜欢”，统一为一个 Books 列表。
- Home、Find Books、Recent 使用相同书目布局：左侧封面，右侧 Title、By、Series、Lv、No.、Words、F/NF、Pages。
- “来源”当前按 Series（系列/合集）展示；出版社、版本、ISBN 和录音来源不能混为同一个字段。

## 已完成

| 模块 | 实际完成程度 |
|---|---|
| UI 与导航 | 11 个前端页面；三个主 Tab；统一配色、封面、列表与主要英文功能文案 |
| 书库 | 6 本样例；书名/系列/作者/编号搜索及类别筛选；词数、页数、等级为待核验样例 |
| 音频 | 彼得兔一章真实 LibriVox MP3，322.026 秒；浏览器实测解码、播放、暂停续播、拖动、倍速、结束事件与跨页继续播放通过 |
| 收藏与记录 | 本地收藏、最近访问听读；跨页共享播放状态；重启从可信断点暂停恢复；覆盖区间去重，倍速按真实耗时统计；跨设备同步尚未完成 |
| Quiz | 彼得兔固定 10 道项目原创双语理解题；每题绑定审核正文 cue；本机反馈明确未验证；版本化 attempt 保存逐题选择；报告按每篇最新兼容记录聚合，满 6 篇才显示趋势；排行不伪造数据 |
| 借阅 | 仅 Demo 保留本机演示预约、重复预约拦截、取消；正式首版不开放 |
| 登录 | 仅 Demo 保留微信/手机号双入口和固定验证码；正式模式不伪装真实鉴权 |
| 内容维护 | JSON 样例目录、稳定 ID、字段校验与本地构建；尚无云端内容发布后台 |
| 验证 | 正式/Demo 双构建审计、可复现哈希、生成产物静态检查、浏览器主交互与 320/390/430px 布局通过；微信真机尚未验收 |

## 优先待办

1. **审核彼得兔同步字幕**：`C-01` 已于 2026-09-21 完成。审核人 `wzx` 完整听完 322.026 秒音频并签署 28/28 个决策：26 个独立低置信词位均保留标准正文，O1/O2 两个录音漏句候选按实听恢复为 c0012（75000–80600 ms）和 c0028（164080–167460 ms）。校验、修订应用和客户端导出均已完成，正式时间轴现为 59 条，`subtitleReviewComplete:true`。
2. **审核并接入 Peter Rabbit 词卡**：`C-02` 和 `X-03` 已于 2026-09-22 完成。371 个审核单元形成 358 个正式义项，已按 token 的上下文 key 接入播放器并覆盖 906 个可点击 token；播放中打开词卡会暂停，关闭后仅在原先播放时续播。审核方式为 `wzx` 授权的 AI 辅助全量审核，不声称 `wzx` 本人逐项人工检查，整体人工完成门槛继续关闭。
3. **统一内容包**：每篇使用稳定 piece_id，音频、字幕、词表、题目及版本号成套关联。书籍与篇章分层；校验英文词数、分级来源、具体版本页数、Series、ISBN/F-NF。不能根据书名或标题猜元数据。
4. **完善播放器状态**：`X-01` 已完成；`X-02` 代码与自动验收已于 2026-09-23 完成。完成判定要求自然结束、唯一覆盖率至少 90% 且尾部 3 秒已听；拖到结尾不会完成或污染可信断点；未完成内容重启后暂停在可信位置；倍速按真实墙钟时间累计，超过 30 秒且位置/倍速不一致的异常后台采样不计入。当前状态为 `PARTIAL_DEVICE_PENDING`，仍需至少一台微信真机验证后台、锁屏和系统控制。
5. **真实账户与云端存储**：接微信服务端身份校验、短信服务、绑定/受控合并；部署数据库与文件存储；进度、收藏、题目记录、借阅跨设备同步。
6. **完成体验版与正式版隔离验收**：`X-05` 的双构建、物理隔离、可复现哈希、静态检查、浏览器回归和微信原生开发版验证已完成；正式开发版本 `0.1.1` 已于 2026-09-23 上传并归档。微信后台当前停留在基本资料初始化页，需先完成名称、头像、简介和适用类目等公开资料，才能进入版本管理并把 `0.1.1` 设为体验版；随后在 Android/iPhone 重复验证并保存两个版本的版本号、截图和记录。完成这些外部证据前 X-05 保持 `PARTIAL`。
7. **运营加书流程**：上传音频与文本，自动校验和处理，预览确认后发布到云端；客户端从 API 拉内容，实现新增书籍不发版。本地 JSON 仍随前端打包。
8. **完善测验、报告与排行**：`X-04` 客户端边界已于 2026-09-23 完成。本机结果标为 `local_unverified`，旧记录只展示不参与新版统计，同一篇只取最新兼容记录，至少 6 篇才显示早期/近期趋势；排行榜未接服务端时只展示不可用状态，不伪造用户、名次或筛选。服务端按 `docs/Quiz-attempt-API-v1.md` 实现判题和验证仍属于后端任务。
9. **微信真机和上线验收**：AppID、合法域名、后台音频权限、锁屏控制、系统打断、弱网、缓存及 iOS/Android 兼容测试。
10. **真实支付与权益（见 `docs/正式产品实施计划-v1.1.md` 的 `P` 任务）**：阶段 0 `GO` 后实施服务端商品/订单/权益、验签、查单补偿、退款/对账；客户端不凭本地状态解锁。
11. **登录与同步（见主计划 `B-03`、`S-01` 至 `S-04`）**：S-01 客户端白名单快照、显式同意、确定性 `snapshotId` 和 mock 幂等边界已经完成；下一步等待 B 冻结 `POST /api/v1/me/local-import`、以 `userId + snapshotId` 幂等处理并返回真实回执。本机 `purchased/coupons`、Demo 状态、可信分数和权益不得迁入。
12. **内容权利与阶段 0（见主计划 `C-03`、§6）**：Peter Rabbit 中国大陆商业使用外审证据包与空白签署模板已就绪，下一步由实际外部审核人签署；原创/Oz 分项权利证据、支付资格与收费适用性仍待确认；所有内容保持 `publishable:false` 至复核通过。
13. **后端契约接入**：`X-06` 通用网络基础层已完成并保持 fail-closed。收到 B 的固定契约版本和提交 SHA 后，先核对 OpenAPI/Schema、token 生命周期、错误码和 test 域名，再实现 `wx.login`、session、content 与 Quiz 业务适配器；不得用 mock 冒充联调完成。

后续开发统一按 `docs/正式产品实施计划-v1.1.md` 的任务 ID、顺序和完成定义执行。C-04、X-03、X-01、X-04 与 X-06 已完成；S-01 客户端基础为 `PARTIAL_BACKEND_PENDING`；X-02 等待单台微信真机后台/锁屏验收后关闭；X-05 自动化范围已完成，等待品牌资料、体验版/正式版 Android 与 iPhone 证据；客户端真实 API 接入等待 B 固定契约版本与 SHA；当前外部动作仍是 C-03 权利审核签署。C-05 及发布仍需等待全部阻断关闭。

具体模块、内容包格式、处理状态、验收规则和批量扩展方案见：`录音-字幕-点词内容管道设计-v1.0.md`。
