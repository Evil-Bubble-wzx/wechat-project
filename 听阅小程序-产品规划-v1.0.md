# 听阅小程序：产品规划

> 版本：v1.0 ｜ 2026-09-16  
> 阶段：规划稿，暂不进入开发  
> 产品范围：数字听阅 + 实体书借阅 + 统一账户体系  
> 配套文档：`听阅小程序-页面设计与技术实现说明书-v1.0.md`（其中数字听阅设计可复用，原“不做物理借阅”的范围声明作废）

---

## 1. 产品定位

这不是一个只播放英文音频的工具，而是一个把**线上听阅学习**与**线下实体书流转**连接起来的小程序。

用户可以：

1. 在线找书、听音频、看字幕、查词并完成 Quiz；
2. 查看实体库存，预约或借阅纸质书；
3. 在“我的”中统一查看听阅进度、当前借阅、到期时间和历史记录；
4. 使用微信或手机号登录，同一个人通过不同方式登录时能够合并为同一账户。

### 1.1 本期的三个核心闭环

| 闭环 | 起点 | 完成标志 |
|---|---|---|
| 账户闭环 | 选择微信登录或手机号登录 | 成功进入小程序，并能找回同一账户的数据 |
| 数字听阅闭环 | 找到一本书或一篇内容 | 完成播放、记录进度并可参加 Quiz |
| 实体借阅闭环 | 查看一本实体书 | 完成预约/借出，能看到到期时间，最终归还 |

### 1.2 可移植性约束

“可移植性”仍按上一版定义：听阅与借阅能力要做成边界清晰的功能模块，后续可以嵌入其他已开发中的小程序；它不等于本期要迁移到 H5 或 App。

建议拆为三个模块：

```text
modules/
├─ account/          # 微信登录、手机号登录、账户绑定与合并
├─ listen-read/      # 数字内容、播放器、字幕、词卡、Quiz、进度
└─ physical-loan/    # 实体库存、预约、借出、续借、归还、借阅记录
```

三个模块只通过稳定的用户 ID、书籍 ID 和接口契约关联，不互相读取内部状态。

---

## 2. 用户角色

| 角色 | 主要能力 |
|---|---|
| 游客 | 浏览公开书目和详情；不能记录进度或提交借阅 |
| 注册用户/家长 | 管理账户、选择孩子、查看报告、预约和查看借阅 |
| 阅读者/孩子 | 听阅、查词、答题、查看个人进度 |
| 馆员/运营人员 | 维护书目与库存、确认取书、借出、归还、逾期处理 |
| 系统管理员 | 门店、借阅规则、账号权限、内容发布和数据统计 |

首版可先采用“一个家长账户对应一个阅读者”。数据模型保留家庭与孩子关系，避免后续增加二孩时重构进度和借阅记录。

---

## 3. 产品信息架构

底部采用四个 Tab：

```text
首页
├─ 搜索
├─ 推荐与分类
├─ 数字可听
└─ 实体可借

书库
├─ 全部书目
├─ 数字听阅
├─ 实体借阅
├─ 难度/年龄/题材筛选
└─ 馆点与库存筛选

借阅
├─ 待取书
├─ 借阅中
├─ 即将到期/已逾期
├─ 预约排队
└─ 借阅历史

我的
├─ 登录与账户绑定
├─ 孩子/阅读者档案
├─ 听阅进度与 Quiz 报告
├─ 收藏与生词本
├─ 借阅卡/借阅资格
├─ 消息通知
└─ 设置与反馈
```

### 为什么从三个 Tab 改为四个

原说明书的“首页 / 最近 / 我的”只适合数字听阅。加入实体借阅后，用户需要频繁确认待取、到期和归还状态，这些信息不能藏在“我的”深层菜单中。因此新增独立的“借阅”Tab；“最近播放”降为首页或数字听阅页中的一个区块。

---

## 4. 登录与账户规划

### 4.1 登录页

首次进行收藏、播放进度同步、Quiz 或实体借阅时进入登录页：

```text
┌──────────────────────────────┐
│          品牌与说明           │
│                              │
│      [ 微信快捷登录 ]         │
│                              │
│      [ 手机号登录 ]           │
│                              │
│  登录即表示同意用户协议与隐私政策 │
└──────────────────────────────┘
```

| 方式 | 用户操作 | 技术方式 |
|---|---|---|
| 微信登录 | 点击一次，确认授权 | `wx.login` 获取临时 code，服务端换取微信身份并签发业务 token |
| 手机号登录 | 输入手机号和短信验证码 | 服务端发送并校验验证码，成功后签发同一种业务 token |

微信登录不能默认取得手机号。需要手机号用于借阅通知时，应在登录后单独请求绑定手机号，不把手机号授权伪装成微信登录的必要条件。

### 4.2 统一账户与防重复

服务端必须有独立的 `user_id`，微信 `openid/unionid` 和手机号只是登录凭证：

```text
user_id
├─ wechat_identity: app_id + openid (+ unionid，如可用)
└─ phone_identity: country_code + phone
```

合并规则：

1. 用户先微信登录、后绑定一个已有账户的手机号时，必须经过短信验证；
2. 两边都有历史数据时，不自动覆盖，执行受控合并；
3. 听阅进度按书/篇合并，保留较新进度和最好 Quiz 成绩；
4. 实体借阅记录必须全部保留，不允许删除或覆盖；
5. 合并动作写入审计日志，出现错误时可追溯。

### 4.3 登录拦截原则

- 浏览书目和详情：允许游客使用；
- 开始试听：可允许游客试听，正式进度同步前提示登录；
- 收藏、Quiz、借阅、续借：必须登录；
- 提交实体借阅前：必须绑定手机号，用于到期和取书通知；
- 不在小程序启动时强制登录，避免降低首次访问转化。

---

## 5. 书目与实体库存模型

数字内容和实体书不能做成两套互不相关的书目。应建立统一“作品—版本—实体册”结构：

```text
作品 Work
├─ 数字内容 Digital Edition
│  ├─ 音频
│  ├─ 字幕/词表
│  └─ Quiz
└─ 实体版本 Physical Edition
   └─ 实体册 Copy
      ├─ 馆点
      ├─ 馆藏条码
      └─ 当前状态
```

| 对象 | 说明 | 关键字段 |
|---|---|---|
| `works` | 用户看到的统一书目 | id, title, author, cover, level, categories |
| `digital_editions` | 可听阅的数字版本 | work_id, audio/content manifest, status, version |
| `physical_editions` | ISBN、出版社等实体版本 | work_id, isbn, publisher, publish_year |
| `library_copies` | 每一本实际可流转的书 | copy_id, edition_id, branch_id, barcode, status |
| `branches` | 馆点/取还点 | name, address, opening_hours, contact |

同一本作品可以只有数字版、只有实体版，或者两者都有。详情页根据数据动态显示“开始听阅”和“预约借阅”，不能假定每本书两种能力都齐全。

---

## 6. 实体借阅规划

### 6.1 用户流程

```text
搜索/浏览书目
  → 查看详情和各馆点库存
  → 选择取书馆点
  → 确认借阅人、手机号和借阅规则
  → 提交预约
  → 馆员备书
  → 用户收到“可取书”通知
  → 到店核销并借出
  → 到期前提醒
  → 续借或到店归还
  → 借阅完成
```

首版按“线下自取、线下归还”规划。邮寄配送涉及地址、运费、物流、丢损和逆向退货，是另一套业务，未确认前不放入首版。

### 6.2 借阅状态

| 状态 | 用户含义 | 可执行动作 |
|---|---|---|
| `reserved` | 已预约，等待馆员处理 | 取消预约 |
| `ready_for_pickup` | 已备好，等待取书 | 查看取书码、导航、取消 |
| `borrowed` | 已借出 | 查看到期日、申请续借 |
| `due_soon` | 即将到期 | 续借或查看归还点 |
| `overdue` | 已逾期 | 查看处理提示、联系馆员 |
| `returned` | 已归还 | 再次借阅、评价 |
| `cancelled` | 已取消 | 再次预约 |
| `expired` | 超过取书保留期 | 再次预约 |

`due_soon` 可以由日期动态计算，不一定单独存库；订单主状态仍保持 `borrowed`。

### 6.3 馆藏册状态

| 状态 | 说明 |
|---|---|
| `available` | 在馆可借 |
| `reserved` | 已被预约锁定 |
| `on_loan` | 已借出 |
| `maintenance` | 修补/消毒/盘点中 |
| `lost` | 丢失 |
| `retired` | 下架，不再流通 |

预约时必须由服务端事务或原子操作锁定具体实体册，不能只在前端判断“库存大于 0”，否则多人同时预约会超借。

### 6.4 借阅规则配置

以下规则不要写死在前端，全部由服务端配置：

- 每个用户最多可借数量；
- 借期天数；
- 可续借次数和续借天数；
- 预约保留天数；
- 逾期后是否暂停新借阅；
- 是否需要押金、会员或有效借阅卡；
- 不同馆点或会员等级是否采用不同规则。

首版如果业务规则还没最终确定，界面读取配置并展示，开发时使用测试配置即可。

### 6.5 馆员操作

小程序用户端之外，至少需要一个轻量馆员工作台，可以先做 Web 管理页：

- 扫描或输入馆藏条码；
- 查看新预约并分配实体册；
- 标记“已备好”；
- 扫取书码完成借出；
- 扫描归还；
- 标记破损、丢失或维护；
- 查询用户当前借阅和历史操作。

没有馆员端，用户端的预约状态无法可靠推进，因此馆员工作台属于实体借阅 MVP，不是后续附加功能。

---

## 7. 关键页面规划

| 页面 | 核心内容 | 首版 |
|---|---|---|
| 启动/登录页 | 微信登录、手机号登录、协议与隐私政策 | P0 |
| 首页 | 搜索、继续听、待取/到期提醒、推荐书目 | P0 |
| 书库页 | 数字/实体筛选，题材、年龄、难度、馆点筛选 | P0 |
| 作品详情页 | 统一书目资料、数字听阅入口、实体库存和借阅入口 | P0 |
| 播放器 | 音频、字幕、点词、播放进度 | P0 |
| Quiz 页 | 答题、结果、进步记录 | P0 |
| 确认借阅页 | 借阅人、馆点、手机号、规则确认 | P0 |
| 借阅 Tab | 待取、借阅中、到期/逾期、预约和历史 | P0 |
| 借阅详情页 | 状态时间线、取书码、到期日、续借/取消 | P0 |
| 我的 | 账户、孩子档案、报告、借阅资格、收藏、生词 | P0 |
| 馆点详情 | 地址、营业时间、联系方式、导航 | P0 |
| 排行榜 | 听阅排行 | P1 |
| 离线下载 | 音频缓存管理 | P1 |
| 笔记/点赞 | 阅读互动 | P1 |

首页应优先显示需要处理的事务：如“1 本书待取”“2 天后到期”，其次才是内容推荐。

---

## 8. 通知规划

需要通知的事件：

- 预约成功；
- 书已备好，可以取书；
- 取书保留期即将结束；
- 到期前提醒；
- 已逾期；
- 续借成功或失败；
- 归还成功。

小程序订阅消息需要用户授权，不能假设永久可推送。关键通知同时保留小程序内消息中心；手机号已绑定且用户同意时，可按业务需要补充短信。通知授权必须按场景申请，不在首次启动时一次性索取全部权限。

---

## 9. 数据与接口边界

### 9.1 核心新增数据表

| 表 | 用途 |
|---|---|
| `users` | 统一业务用户 |
| `user_identities` | 微信、手机号登录凭证 |
| `families` / `readers` | 家庭与阅读者档案 |
| `works` | 统一作品书目 |
| `digital_editions` | 数字内容版本 |
| `physical_editions` | 实体出版版本 |
| `library_copies` | 可流转的具体馆藏册 |
| `branches` | 馆点/取还点 |
| `reservations` | 预约记录 |
| `loans` | 正式借出、续借与归还记录 |
| `loan_events` | 不可覆盖的状态变更审计日志 |
| `notifications` | 站内消息与发送状态 |

### 9.2 首批接口

```text
POST /auth/wechat
POST /auth/phone/send-code
POST /auth/phone/verify
POST /auth/bind-phone
POST /auth/merge

GET  /works
GET  /works/:id
GET  /works/:id/availability

POST /reservations
GET  /reservations/me
POST /reservations/:id/cancel

GET  /loans/me
GET  /loans/:id
POST /loans/:id/renew

GET  /branches
GET  /branches/:id
```

馆员端借出和归还接口必须单独鉴权，不能复用普通用户权限。

---

## 10. MVP 分期

### 阶段 0：规则确认与原型

- 确认馆点、借期、续借、取书保留期、押金/会员规则；
- 确认一个账户是否支持多个孩子；
- 完成登录、统一详情页、借阅状态和四 Tab 低保真原型；
- 确认首批试点书目及每本书的数字/实体可用情况。

### 阶段 1：账户与实体借阅最小闭环

- 微信登录、手机号验证码登录、绑定与账户合并；
- 统一书目、实体版本、实体册和馆点；
- 库存查询、预约、备书、取书核销、借出、归还；
- 借阅 Tab、到期时间、取消与续借；
- 轻量馆员工作台和操作日志；
- 站内消息，接入必要的订阅消息。

### 阶段 2：数字听阅闭环

- 播放器、后台播放、字幕、点词、进度；
- Quiz、报告、收藏和生词本；
- 数字内容导入管道；
- 数字听阅与统一作品详情页打通。

阶段 1 与阶段 2 可由不同开发人员并行，但必须先统一 `user_id` 和 `work_id`。若只有一人开发，建议先跑通登录和实体借阅骨架，再接入已经设计较完整的数字听阅模块。

### 阶段 3：运营增强

- 离线音频、推荐、排行榜；
- 多孩子档案；
- 会员、押金或支付（如业务需要）；
- 更多馆点和跨馆还书；
- 邮寄配送（仅在业务明确后单独立项）。

---

## 11. MVP 验收标准

### 账户

- 用户可明确选择微信或手机号登录；
- 两种登录最终获得同一种业务 token 和统一 `user_id`；
- 微信账户绑定已有手机号时不会生成两个用户；
- 更换设备后可恢复听阅和借阅数据。

### 实体借阅

- 用户能看到具体馆点的可借数量；
- 两人抢最后一本书时最多一人预约成功；
- 馆员能完成备书、借出和归还；
- 用户端状态与馆员操作一致；
- 到期、逾期、续借和取消规则由服务端配置控制；
- 每次状态变化都有不可覆盖的审计记录。

### 数字听阅

- 能从统一作品详情进入播放器；
- 播放进度、Quiz 和报告绑定统一阅读者；
- 后续新增书和音频不修改前端代码、不重新发版；
- 听阅模块可通过 Adapter 移植到另一个小程序。

---

## 12. 开工前必须确认的业务项

以下事项不影响先画原型和建数据模型，但在实体借阅上线前必须确定：

1. 首版有几个馆点，是否允许跨馆归还；
2. 是到店自取/归还，还是首版就包含邮寄；
3. 默认借期、借阅上限、续借次数和预约保留期；
4. 是否收会员费、押金、逾期费或丢损赔偿；
5. 一个家长账户是否需要管理多个孩子；
6. 馆员使用 Web 工作台，还是另做馆员版小程序；
7. 手机号登录采用短信验证码，短信服务商及预算；
8. 微信登录后是否在首次借阅时强制绑定手机号。

在这些规则未确认前，不应直接按原页面说明书开工，因为导航、首页优先级、用户模型和数据表都已经发生变化。
