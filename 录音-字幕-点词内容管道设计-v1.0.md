# 听阅内容管道：录音 → 完整字幕 → 点词

> 版本：v1.0 ｜ 2026-09-17  
> 目标：先用《The Tale of Peter Rabbit》跑通一篇，随后用同一套流程批量处理更多书。  
> 原则：客户端不在运行时生成字幕或释义；所有内容在发布前完成生成、校验和人工审核。

---

## 1. 核心判断

这条链不能按“一个音频文件 + 一个字幕文件 + 临时查词接口”分别开发。它们必须属于同一个稳定的 `piece_id` 和 `content_version`：

```text
piece_id
├─ audio          实际播放的录音
├─ canonical_text 与录音版本对应的标准原文
├─ cues           原文切句及时间轴
├─ tokens         每句话的可点击单词位置
├─ vocab          结合原句生成的词义
└─ qa_report      自动校验及人工审核记录
```

后续增加一本书时，书只是容器；真正进入生产流水线的单位是“篇/章”。一本 20 章的书就是 20 个可独立重试、审核和发布的 piece。

---

## 2. 整体流程

```text
录音 + 对应版本原文 + 来源信息
              │
              ▼
① ingest      创建稳定 book_id / piece_id，校验必填字段与权利来源
              │
              ▼
② probe       探测时长、编码、声道、采样率，计算 SHA-256
              │
              ▼
③ normalize   转成统一发布规格；清理文本格式，但不改写原文
              │
              ▼
④ transcribe  ASR 生成带单词时间的草稿，用于定位录音内容
              │
              ▼
⑤ align       将“核验过的标准原文”强制对齐到录音时间轴
              │
              ▼
⑥ segment     按句子和可读长度生成字幕 cues，标记片头/正文/片尾
              │
              ▼
⑦ tokenize    将每句拆成 word / punctuation，并保留出现位置
              │
              ▼
⑧ vocab       词形还原、词性、上下文义项、音标、中英释义、专名规则
              │
              ▼
⑨ validate    自动检查音频、时间轴、文本覆盖、词卡覆盖和版本一致性
              │
              ▼
⑩ review      人工试听抽检并修正；审核通过后才允许发布
              │
              ▼
⑪ publish     原子发布完整内容包，客户端按版本下载与缓存
```

Whisper 的 `word_timestamps` 可以产生单词级时间信息，适合生成对齐草稿；最终显示文本仍以核验后的原文为准，不能直接把 ASR 猜出的文字当正式字幕。

---

## 3. 标准内容包

```text
content/
└─ peter-rabbit/
   └─ peter-rabbit-01/
      ├─ source/
      │  ├─ audio-original.mp3
      │  ├─ text-original.txt
      │  ├─ rights.json
      │  └─ manifest.source.json
      ├─ work/                       # 可删除重建，不发布
      │  ├─ audio-normalized.mp3
      │  ├─ audio-probe.json
      │  ├─ asr-words.json
      │  ├─ alignment.json
      │  └─ unmatched.json
      ├─ dist/                       # 客户端实际使用
      │  ├─ manifest.json
      │  ├─ audio.mp3
      │  ├─ cues.json
      │  ├─ vocab.json
      │  └─ qa-report.json
      └─ review/
         └─ corrections.json
```

`source/` 永不被自动覆盖；`work/` 可以按输入哈希重新生成；`dist/` 只包含审核通过的发布产物。

### 3.1 源清单

```json
{
  "schemaVersion": 1,
  "bookId": "peter-rabbit",
  "pieceId": "peter-rabbit-01",
  "index": 1,
  "title": "The Tale of Peter Rabbit",
  "language": "en",
  "audio": "source/audio-original.mp3",
  "text": "source/text-original.txt",
  "properNouns": ["Peter", "Flopsy", "Mopsy", "Cotton-tail", "McGregor"],
  "rights": "source/rights.json"
}
```

### 3.2 发布清单

```json
{
  "schemaVersion": 1,
  "pieceId": "peter-rabbit-01",
  "contentVersion": 1,
  "status": "published",
  "language": "en",
  "audio": {
    "url": "https://cdn.example.com/peter-rabbit-01/v1/audio.mp3",
    "durationMs": 322026,
    "sha256": "..."
  },
  "artifacts": {
    "cues": "cues.json",
    "vocab": "vocab.json"
  },
  "counts": {
    "cues": 0,
    "tokens": 0,
    "clickableTokens": 0,
    "vocabEntries": 0
  }
}
```

客户端以 `pieceId + contentVersion` 作为缓存键。音频、字幕或词表任一改变，都生成新版本，避免旧音频配到新字幕。

---

## 4. 字幕与单词的数据契约

### 4.1 `cues.json`

```json
[
  {
    "id": "c0001",
    "kind": "intro",
    "startMs": 0,
    "endMs": 15200,
    "text": "This is a LibriVox recording...",
    "translation": null,
    "tokens": []
  },
  {
    "id": "c0002",
    "kind": "content",
    "startMs": 15200,
    "endMs": 21500,
    "text": "Once upon a time there were four little rabbits.",
    "translation": "从前，有四只小兔子。",
    "tokens": [
      { "id": "c0002:t00", "surface": "Once", "lemma": "once", "kind": "word", "vocabKey": "once:adv:1" },
      { "id": "c0002:t01", "surface": " ", "kind": "space" },
      { "id": "c0002:t02", "surface": "upon", "lemma": "upon", "kind": "word", "vocabKey": "upon:prep:1" }
    ]
  }
]
```

要点：

- 时间统一使用整数毫秒，避免浮点误差；
- `kind` 区分 `intro / content / outro`，LibriVox 片头不能和正文混在一起；
- `text` 必须能由 tokens 按顺序无损拼回；
- 点词传递的是 token 出现 ID 与 `vocabKey`，不能只传字符串 `garden`；
- P0 只需句级时间，token 不必都有时间；若后续做卡拉 OK，再给 word token 增加 `startMs/endMs`。

### 4.2 `vocab.json`

```json
{
  "garden:n:1": {
    "lemma": "garden",
    "phonetic": "/ˈɡɑːdn/",
    "partOfSpeech": "noun",
    "definitionZh": "花园；园子",
    "definitionEn": "a place where flowers or vegetables grow",
    "exampleCueId": "c0048",
    "properNoun": false,
    "image": null,
    "reviewStatus": "approved"
  }
}
```

同一单词在不同原句可能含义不同，因此 key 至少包含 lemma、词性和义项编号。义项在内容构建期根据原句确定，用户点词时只查本地/服务端已发布词表，不实时调用 LLM。

---

## 5. 对齐策略

### 5.1 为什么要同时有原文与 ASR

只使用 ASR 会出现拼错专名、漏词、重复句和标点不稳定；只使用电子书原文又不知道每句话在录音里的时间。因此采用：

```text
标准原文决定“显示什么”
ASR 单词时间决定“大致在哪里”
强制对齐决定“准确时间”
人工审核处理无法自动判断的差异
```

### 5.2 版本不一致的处理

录音可能包含朗读者增加的片头、章节名、口误或与电子书不同的标点。管道必须输出差异报告：

- `audio_only`：录音有、原文没有，例如 LibriVox 片头；
- `text_only`：原文有、录音跳过；
- `substitution`：录音与原文用词不同；
- `low_confidence`：时间或识别置信度不足。

不能为了让自动检查通过而悄悄改原文。审核人员需要明确决定：保留为片头、采用朗读版本文字、标记不可点击，或更换音频/原文来源。

### 5.3 字幕切句规则

- 优先按原文句号、问号、感叹号切句；
- 过长句在逗号、分号或自然停顿处分段；
- 每条建议 4–16 个英文词，以手机屏幕 1–3 行为宜；
- 同一句的相邻分段保持连续，不能丢标点；
- 客户端显示当前句前 2 条、后 4 条，共 5–7 行窗口；
- 英文字幕为主，中文翻译作为独立可选层，不影响点词定位。

---

## 6. 模块划分

### 6.1 内容生产工具

```text
tools/content-pipeline/
├─ cli                     # build / validate / review / publish / batch
├─ ingest                  # ID、清单、权利信息
├─ audio                   # ffprobe、转码、哈希
├─ transcript              # 原文清洗、ASR
├─ align                   # 原文与录音强制对齐
├─ segment                 # 字幕切句
├─ tokenize                # 单词、标点、位置与词形还原
├─ vocab                   # 上下文义项、释义、音标、专名规则
├─ qa                      # 自动验收和报告
└─ publish                 # 上传、写库、版本切换
```

每个步骤都应满足：

- 输入和输出格式固定；
- 使用输入哈希缓存，可断点续跑；
- 重复执行结果一致；
- 单步失败不污染已发布版本；
- 记录工具版本、参数、耗时和错误。

### 6.2 小程序运行模块

```text
modules/listen-read/
├─ content-repository      # 拉取 manifest/cues/vocab，按版本缓存
├─ player-session          # 音频状态、续听、seek、倍速、后台播放
├─ subtitle-engine         # 根据 currentTime 二分查找当前 cue
├─ subtitle-window         # 当前句前 2 后 4 的展示窗口
├─ token-renderer          # 逐 token 渲染，处理点击
├─ word-card               # 按 vocabKey 显示词卡
└─ progress                # 真实收听秒数与完成状态
```

模块边界：播放器只上报 `currentTime`；字幕引擎只读取 cues；点词组件只接收 token 与 vocab。三者不读取页面内部变量，方便整体移植。

---

## 7. 客户端运行过程

```text
进入详情页
  → 预取 manifest
  → 进入播放器并启动音频
  → 并行读取 cues + vocab
  → 每次 onTimeUpdate 用二分查找当前 cue
  → 只更新 cueIndex 和 5–7 行窗口
  → 用户点击 word token
  → 记录点击前是否正在播放
  → 暂停音频
  → 用 vocabKey 打开词卡
  → 关闭词卡时按原播放状态决定是否续播
```

降级规则：

- manifest 拉取失败：显示加载失败，不播放错版本内容；
- cues 缺失：可以播放音频，但字幕按钮显示不可用；
- 某个 vocabKey 缺失：该 token 暂不可点，并上报内容错误；
- 中文翻译缺失：英文字幕和点词仍可正常工作；
- 缓存版本不一致：丢弃整套旧内容包，不允许混用文件。

---

## 8. 自动验收与人工审核

### 8.1 自动验收

发布前必须全部通过：

- 音频可解码，实际时长与 manifest 一致；
- 所有 cue 满足 `0 ≤ startMs < endMs ≤ durationMs`；
- cue 时间单调递增，重叠和异常长空白有报告；
- cues 拼接后的正文与 canonical text 一致；
- ASR 与原文的匹配率达到设定阈值，未匹配项全部进入报告；
- token 拼接后与 cue.text 完全一致；
- 每个可点击 token 都有 vocabKey，每个 vocabKey 都有词表项；
- 专有名词、停用词和不可点规则已应用；
- 音频、cues、vocab 的 pieceId/contentVersion 完全一致；
- 所有文件生成哈希，发布清单可复核。

### 8.2 人工审核

首批每篇建议：

- 完整听一遍短篇；长篇至少检查开头、中间、结尾及自动标记的低置信度区间；
- 随机抽查不少于 10 句，目标句级误差 ≤300ms；
- 检查片头进入正文的边界和最后一句；
- 抽查专有名词、词形还原与多义词；
- 确认页面点词显示的是本句义项；
- 由审核人签名并记录审核时间，才能发布。

第一篇不要只抽查；彼得兔应完整听完并逐项修正，用它建立今后的审核基准。

---

## 9. CLI 预期用法

```text
content-import init peter-rabbit/peter-rabbit-01
content-import validate-source content/peter-rabbit/peter-rabbit-01
content-import build content/peter-rabbit/peter-rabbit-01
content-import review content/peter-rabbit/peter-rabbit-01
content-import validate-dist content/peter-rabbit/peter-rabbit-01
content-import publish content/peter-rabbit/peter-rabbit-01 --environment staging
content-import batch content/inbox --workers 4
```

状态机：

```text
draft → source_validated → processing → needs_review
      → approved → staged → published
      ↘ failed（修正后从失败步骤继续）
```

发布采用两步：先上传带版本号的所有文件并校验，再原子切换数据库中的 `published_version`。客户端永远看不到半套内容。

---

## 10. 从一本扩到大量书

### 第一阶段：彼得兔完整样板

1. 找到与当前录音逐字对应的公版英文原文；
2. 明确片头、正文、片尾并建立 corrections；
3. 生成并人工校正全篇 cues；
4. 生成全部 tokens 和上下文词表；
5. 客户端接入真实 cues/vocab，替换写死的 garden；
6. 完成自动验收、整篇人工试听和 staging 发布。

### 第二阶段：再选 3 篇验证差异

选择三种不同情况：

- 录音与原文高度一致；
- 有长句、人物对话和多个专有名词；
- 有明显片头、章节名或录音与文本差异。

三篇均无需修改客户端代码，只修改内容和审核修正文件，才算真正模块化。

### 第三阶段：10–20 篇批量生产

- 批量队列运行自动阶段；
- 审核人员只处理低置信度和自动报告；
- 失败篇目单独重试，不阻塞其他内容；
- 统计每篇自动匹配率、人工修改量和处理时长；
- 根据数据决定是否建设 Web 内容审核台。

### 第四阶段：内容运营后台

运营人员上传音频、原文、封面和来源证明，后台展示处理状态、字幕时间轴、低置信度区间与词卡预览；审核通过后一键发布。客户端只从 API 读取已发布版本，新增书籍不需要重新提交小程序。

---

## 11. 粗略排期

以 1 名熟悉前后端和音频工具的开发者估算：

| 工作 | 估算 |
|---|---:|
| 内容包 schema、CLI 骨架、音频探测与版本校验 | 1–2 天 |
| 彼得兔原文核对、ASR/对齐、全篇审核 | 1–2 天 |
| 客户端字幕引擎、窗口渲染、seek 联动 | 1–2 天 |
| tokens、词表和真实点词接入 | 2–3 天 |
| 自动 QA、发布流程及三篇回归 | 2–3 天 |
| **完成可复制的首条链路** | **约 7–10 个工作日** |

流水线稳定后，5–10 分钟短篇的机器处理通常可在分钟级完成，人工审核量预计每篇 10–30 分钟；实际速度取决于录音质量、原文匹配度、词表要求和硬件，需用前 3–5 篇实测后再定产能。

---

## 12. 当前项目与目标状态的差距

当前已经有：彼得兔真实音频、播放器、静态字幕区域、固定 garden 词卡和本地生词本。

当前缺少：对应版本的完整原文、全篇时间轴、真实 token 数据、上下文词表、字幕引擎、内容包版本校验、自动 QA、人工审核记录以及原子发布服务。

下一步应先产出彼得兔的 `source/` 内容包与可核对的完整英文原文，再做对齐。没有确认原文版本之前，不应直接批量生成字幕和词表。
